# UX-11 方案设计：P2-03 表格键盘与读屏语义

> 状态：待 3 轮方案检视
> worktree：`.worktrees/fix-ux-11-table-a11y`（分支 `fix/ux-11-table-a11y`）

## 1. 问题陈述

UX 检视报告 P2-03「自定义表格的键盘与读屏语义不足」：

- 表格用 Column + Row + GestureDetector，非原生 DataTable 语义。
- 排序状态只在标题追加 `↑/↓`，无可程序化感知的语义状态（缺失 aria-sort 等价物）。
- 行高 30px（高于 WCAG 2.2 的 24px 最低目标，但对高频点击与系统缩放偏紧）。
- 既有 E2E 语义制品出现空文本/空 aria_label 按钮节点（源码 tooltip ≠ 语义树可读）。
- 建议：建立键盘契约（Tab 进入、方向键移动、Enter 打开、Space 选择、排序状态朗读）；**若 Flet 能力不足，至少提供等价的可访问详情入口与排序按钮文字**。

Plans.md UX-11 验收：**排序状态可被辅助技术感知（semantics 断言）；键盘方案有 flet-mcp API 证据支撑或降级方案落地；行高 30→≥32**。

## 2. 现状调查与能力验证（实读源码 + flet-mcp）

### 2.1 表格实现现状（`ui/components/virtual_table.py`）

- 表头 `_build_header`（L161-227）：排序表头在 **label 文本后追加 `" ↑"/" ↓"`**（L186-187），GestureDetector(on_tap) 触发 `on_sort(col_id, new_asc)`。
- 数据行 `_build_row`：ROW_HEIGHT=30（L38），GestureDetector(on_tap) → `on_row_click`（详情入口已存在）。
- 既有测试断言表头 `text.value == "Change ↑"` 等（test_virtual_table_body.py L177-187）与 `h.content.on_tap`（L203-223）——**语义包装将要求既有测试同步更新**。

### 2.2 Flet 0.86.5 能力验证（flet-mcp get_api）

| 能力 | 验证结果 | 结论 |
|---|---|---|
| `ft.Semantics` 控件 | 存在：支持 `label`/`tooltip`/`value`/`button`/`on_did_gain_accessibility_focus` 等 | **可用于排序表头语义标注**（读屏朗读"排序按钮 + 当前状态"） |
| `ft.DataTable` | 存在：原生 `sort_ascending`/`sort_column_index`（aria-sort 等价） | 但受限于 Material 设计行内边距/checkbox 列、无虚拟滚动/列宽拖拽——迁移成本高且破坏性能路径，**不做**（报告亦允许降级） |
| 表格级键盘导航（Tab 进入/方向键移动/Enter 打开） | flet 无 grid/focus 遍历控件；`KeyboardListener`（on_key_down 单键事件）无 focus 管理，需自维护焦点且与列宽拖拽/水平滚动冲突 | **无原生能力，降级登记**（报告允许"至少排序按钮文字"） |
| 锚点机制 `anchored()` | E2E 模式包 `ft.Semantics`、**生产 build 为 no-op**（anchor.py L49-50) | 排序语义标注需**生产环境显式 `ft.Semantics`**，与 anchored 不冲突（E2E 下嵌套两层 Semantics，外层 anchor 内层排序描述，互不干扰） |

### 2.3 键盘契约降级判定（flet-mcp 证据）

Flet 0.86.5 **无**：单元格/行 focus 管理（无 focusable 遍历）、方向键导航事件（KeyboardListener 仅有单键 on_key_down，无 grid 语义）、DataTable 键盘遍历（Flet DataTable 未暴露 on_key 风格列/行导航）。

→ **降级落地**（报告允许路径）：排序表头 Semantics(button + 状态描述) 使读屏感知排序方向与可交互性；数据行详情入口既有（行点击打开详情）；行高提升至 32。**完整方向键导航登记技术债**（NOTE(lazy:) 三要素 + 方案登记），升级触发：Flet 提供表格 focus/grid 能力或 KeyboardListener 组合方案经验证。

## 3. 技术方案（最小可行）

### 3.1 代码改动

**`ui/components/virtual_table.py`：**

1. `ROW_HEIGHT = 30 → 32`（Plans 要求 ≥32；数据行点击目标增大，缓解高频点击）

2. `_build_header` 排序表头语义标注（on_sort 非空时，**三态定稿**——检视 CRITICAL/MAJOR-1 修订）：

```python
# 三态语义描述 (检视定稿): 升序 / 降序 / 非当前列可排序
if sort_col == col_id and sort_asc:
    state_desc = I18n.get("table_sort_asc")
elif sort_col == col_id:
    state_desc = I18n.get("table_sort_desc")
else:
    state_desc = I18n.get("table_sort_action")  # "可点击排序"
semantics_label = f"{label}，{state_desc}"
# 注意: 不设 container=True — 保留与 E2E anchored(COMPLEX) 节点的合并可能
# (PoC A7: Semantics 与 GD 合并为单节点, label 落 textContent; container 会阻断合并)
gesture = ft.Semantics(
    button=True,
    label=semantics_label,
    tooltip=semantics_label,
    content=ft.GestureDetector(content=content, on_tap=_make_sort_handler(...)),
)
```

- 视觉 Text 保持 `Change ↑/↓` 不变（既有视觉与测试兼容）。
- **Semantics.button=True 对 GestureDetector 为引擎忽略**（e2e_ids.py L36-38 项目自证），语义可达性由 **label 落 textContent** 保证——不在方案中把 button 当 role 依据。
- 非排序表头（on_sort=None）不包 Semantics，零干预；只读表格语义缺口登记 follow-up（§3.2）。

3. **键盘导航降级登记**：模块级 `# NOTE(lazy:)` 三要素，显式单列「键盘用户无方向键/Enter 详情入口」（最强降级），升级触发：Flet 提供表格 focus/grid 能力或 KeyboardListener 组合方案经 E2E 验证。

**`locales/zh_CN` + `en_US` strings.json：** 新增 3 key（三态齐备）：
- `table_sort_asc`：「升序排列，点击切换为降序」/ "Ascending, click for descending"
- `table_sort_desc`：「降序排列，点击切换为升序」/ "Descending, click for ascending"
- `table_sort_action`：「可点击排序」/ "Click to sort"

### 3.2 明确不改（YAGNI / 评估后排除）

- **DataTable 迁移**：无虚拟滚动/列拖拽，性能与功能回归风险高；报告允许降级（§2.2）。
- **完整键盘契约（方向键移动/Enter 打开/Space 选择）**：Flet 无原生能力（§2.3 附源码锚点：无 Focus 控件、DataTable 无 on_key、KeyboardListener 无 focus 遍历），降级登记；**显式登记最强降级**：当前键盘用户无方向键行导航与 Enter 详情入口（行点击详情仅鼠标/触屏可达），升级触发见 NOTE(lazy:)。
- **行点击 Semantics 细化**：行内 Column 的 Text 自动进 textContent，行 GestureDetector 已有 flt-tappable 语义；不额外包（避免节点膨胀）。
- **非排序表格（on_sort=None）的表/列语义**：本次零干预；「Column+Row 非 DataTable 语义」根因登记 follow-up（P2-03 融合项，非本批验收）。
- **拖拽把手（`exclude_from_semantics=True` 既有契约）**：保持。

### 3.3 边界与一致性（检视 MAJOR-2 修订）

- **E2E 兼容（核心风险）**：column_header 锚点为 `AnchorKind.COMPLEX`（e2e_ids.py L84-94），定位走 `textContent 前缀匹配 + role=button`（anchor_page.py L235-238），**非** aria-label。故内层排序 Semantics **不设 `container=True`**（container 阻断与 anchored 节点的合并——PoC A7 仅覆盖单层嵌套，双层容器行为未证）；合并后 EID/textContent 形态由 E2E 探针实证（DoD 必跑 `test_screener_flow` 排序断言，见 §5）。措辞更正：anchored 为**外层包裹**（非 Stack）。
- **既有测试同步**（test_virtual_table_body.py）：
  - `test_with_on_sort_attaches_click_handler` / `test_on_sort_handler_invokes_callback_with_new_asc` / `test_on_sort_handler_same_column_toggles`：`h.content` 由 GestureDetector 变 Semantics → 改 `h.content.content.on_tap`（穿透一层）。
  - `test_no_sort_col_label_plain` / `test_sort_col_ascii_appends_*` / `test_label_falls_back_to_id` / `test_header_text_uses_*`：on_sort=None/非排序路径不包 Semantics，断言不变。
  - drag 相关用例（on_sort=None 调用）+ `_build_cells/_build_row/组件体`：零影响。
- **测试断言定稿**（检视 MAJOR-2 修订）：不依赖中文字面量，用 `assert I18n.get("table_sort_asc") in sem.label`（期望值同源 I18n.get，翻译变化自动跟随）。注意：`test_virtual_table_body` 的 `mock_i18n_state` 不 patch `I18n.get`，断言解析真实 strings.json（默认 zh_CN）——实现与断言同源即可。

## 4. 测试设计

### 4.1 新增/更新测试

| 测试文件 | 断言 |
|---|---|
| `tests/unit/ui/test_virtual_table.py` | 新增 `ROW_HEIGHT == 32` 断言（需 import ROW_HEIGHT） |
| `tests/unit/ui/test_virtual_table_body.py`（更新既有 + 新增） | ① 更新 3 用例：`h.content` 为 Semantics 时 `h.content.content.on_tap` callable 且调用回调；② 新增 `TestBuildHeaderSemantics`：升序列 `assert I18n.get("table_sort_asc") in sem.label`；降序列同源断言 desc；非当前可排序列 `assert I18n.get("table_sort_action") in sem.label`（三态精确配对）；on_sort=None 序列表头非 Semantics（保持 Container） |
| i18n 一致（既有 tests/unit/test_i18n*.py） | 新 3 key zh/en 双语言齐全自动覆盖 |

### 4.2 既有测试影响

- test_virtual_table_body.py 3 个 on_sort 路径用例需穿透 Semantics（§3.3）。
- 其余表格用例（_build_cells/_build_row/组件体/拖拽）不受 ROW_HEIGHT 与表头影响（列宽/行内容不变），预期零回归。
- E2E：表头 Semantics label 新增不影响既有 anchor 定位（外层 anchor 不变）；无新 E2E 断言。

### 4.3 覆盖

新增分支 = 排序表头 Semantics 包装（on_sort 非空 × 升/降 × 无排序）；测试断言 label 两态 + 结构三态（Semantics/GestureDetector/Container）。ROW_HEIGHT 常量断言直cover。按项目单文件 ≥80% 门槛自检。

## 5. 验收标准（DoD）

- [ ] `ROW_HEIGHT = 32`（≥32 验收）
- [ ] 排序表头 Semantics 三态 label（升/降/可排序）——semantics 断言 TestBuildHeaderSemantics 覆盖三态
- [ ] 键盘完整契约降级登记（NOTE(lazy:) 三要素 + flet 源码锚点证据在 PR body）
- [ ] i18n 新 3 key 双语齐全；既有虚拟表测试更新通过
- [ ] **E2E 排序回归**（检视 MAJOR-2）：跑 `tests/e2e/test_screener_flow.py` 排序相关断言（click_column_header + ↑/↓ 文本）——本地可跑则跑，沙箱受限则记录「待 CI 验证」并说明（不得声称已验证）——验证 Semantics 包装未破坏 COMPLEX 锚点定位
- [ ] ruff + pyright + pre-commit 全过；全量 UI 单测零回归；单文件覆盖率 ≥80%

## 6. 风险与兼容

1. **视觉**：行高 30→32 数据区略加高（虚拟滚动，行数不变，滚动区略增，无功能影响）；表头视觉不变（label + 箭头保留）。
2. **语义树**：新增 Semantics 节点（表头列数级，非行级），树规模可控；拖拽把手 exclude_from_semantics 保持，不污染。
3. **E2E**：anchor 外层定位不受内层排序 Semantics 影响。
4. **既有测试**：仅 test_virtual_table_body.py 3 个 on_sort 路径需穿透更新，其余零回归。
5. **键盘契约**：完整方向键导航为 Flet 能力缺口（flet-mcp 证据），降级为排序状态朗读 + 行高提升；登记技术债（升级触发见 NOTE(lazy:)）。

## 7. 变更文件清单（预计）

- `ui/components/virtual_table.py`（ROW_HEIGHT=32 + 排序表头三态 Semantics + NOTE(lazy:)）
- `locales/zh_CN/strings.json` + `locales/en_US/strings.json`（+3 key）
- `tests/unit/ui/test_virtual_table.py`（import ROW_HEIGHT + ==32 断言）
- `tests/unit/ui/test_virtual_table_body.py`（3 用例结构更新 + 新增 TestBuildHeaderSemantics）
- 无 DB、无迁移

## 8. 实施后检视记录（2 轮：合规 APPROVE + 对抗 REQUEST_CHANGES→已处理）

- **抗对抗检视采纳**：
  - 移除 `Semantics(tooltip=semantics_label)`：label 已承载读屏语义，同串 tooltip 冗余且并入语义树增加双重朗读风险（非方案验收项）。
  - 分隔符随 locale：新增 i18n key `table_sort_sep`（zh `，` / en `, `），`semantics_label = f"{label}{sep}{state_desc}"`，避免英文下全角逗号跨语言混排。→ i18n 新增共 4 key。
- **E2E 排序回归（DoD）→ 标记「待 CI 验证」**：本机环境未安装 playwright（`ModuleNotFoundError`），无法本地跑 `tests/e2e/test_screener_flow.py` 排序断言。双层嵌套 Semantics（外层 anchored COMPLEX + 内层 button=True）合并行为 PoC A7 未实证——已通过移除 tooltip 降低节点膨胀风险；**正式实证需 CI 跑 E2E**，不得默认声称已验证。
- 已本地验证：单元测试 138 passed（virtual_table/virtual_table_body/i18n 完整性）、pre-commit（staged）全过、全量 UI 单测 4426 passed。覆盖率见交付记录。