# Python Runtime
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Distribution / Packaging
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
.venv/
venv/
env/

# IDE / Editor
.idea/
.vscode/
.trae/

# Build & Bundling tools
*.spec
!AStockScreener.spec
flet-app.tar.gz
/VERSION

# Installer logs & pip
pip-log.txt
pip-delete-this-directory.txt
pip/

# Node.js (CI tools: pyright)
node_modules/

# pip-tools: requirements*.txt are auto-generated by pip-compile from pyproject.toml
# Do NOT edit them manually; edit .in files or pyproject.toml instead
# These files SHOULD be committed to version control for reproducible builds

# uv lock file (not needed since we use universal requirements*.txt)
uv.lock

# Unit test / coverage reports / Cache
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
coverage.json
*.cover
.coveragerc.tmp
.hypothesis/
.pytest_cache/
.ruff_cache/
.import_linter_cache/
junit-*.xml

# Coverage report archives (CI uploads to Codecov as authoritative gate)
docs/coverage/

# Test runtime helpers (auto-generated, not source code)
sitecustomize.py

# Gemini / Local AI Assistant
.gemini/

# ==========================================
# Project Specific (AStock Screener)
# ==========================================

# 1. Databases & Settings
astock.db
astock.db-*
user_settings.json

# 2. Logs & Temporary Files
logs/
*.log
*.bak
*.html
mylog*.txt

# 2.1 AI assistant & CI diagnostic artifacts (commit/PR/调查临时文件)
tmp_*.txt
tmp_*.md
tmp_*.json
tmp_*.py
tmp_*.zip
tmp_*/
/tmp/
ci_log_*.txt
codeql_logs.txt
e2e_reports.zip
temp_pr*.txt
.COMMIT_MSG*
*.COMMIT_MSG*
.pr-body*
*.pr-body*
# CI/E2E 调试临时产物（f1_* 来自 gh run view，main_e2e_* 来自 E2E 调试，ruff_diff 来自格式化预览）
f1_*.txt
f1_*.log
main_e2e_*.txt
main_e2e_*.zip
ruff_diff.txt

# 3. Environment Variables & Secrets
.env
.env.*
!.env.example
.secret.key
.secret.key.bak
.secret.legacy
.secret.salt

# 4. Data files (Quantitative/Cache prevention)
*.csv
*.xlsx
*.h5
*.parquet
*.gguf

# 5. E2E test diagnostics
e2e-artifacts/

# 6. Worktrees
.worktrees/

# 7. AI assistant working state (harness-plan / harness-review products)
Plans*.md
.claude/
/spec.md

# .claude-plugin/ generated by `harness sync` from harness.toml (source of truth)
.claude-plugin/

# 8. Code reviews (仅忽略根目录的本地 review 工作产物，docs/reviews/ 不受影响)
/reviews/

# 9. Static analysis reports (auto-generated, not source code)
pyright_report.txt
