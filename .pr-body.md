## 🎯 PR 概述
实现关注列表功能（FR-UX-004, Task 4.2）：用户可从选股结果/详情对话框加入关注，跨日跟踪关注的股票。新增 watchlist 表 + Alembic 迁移 + DAO + ViewModel + View + 导航入口。

## 🔗 关联工单/Issue
Relates to Plans.md Task 4.2（UX/URD 对齐批次）
Relates to #340（URD v2.6 FR-UX-004 产品方案）

## 📌 修改类型（删除无关项）
- [x] feat：新增业务功能
- [x] db：数据表迁移、字段/索引、约束变更（需附alembic脚本）

## 📝 详细变更清单
- **数据层**：
  - `data/persistence/models.py`：新增 `Watchlist` ORM 模型（id/ts_code 唯一/stock_name/added_at/note）
  - `alembic/versions/0016_add_watchlist_table.py`：新增迁移 0016←0015，guard 幂等 + 完整 downgrade
  - `data/persistence/daos/watchlist_dao.py`：新增 DAO（add_to_watchlist/remove_from_watchlist/get_watchlist/is_in_watchlist）
  - `data/data_dictionary.py`：TABLE_DEFINITIONS 注册 watchlist（R12）
  - `data/cache/cache_manager.py`：_DAO_REGISTRY + __init__ + _create_engine + 4 委托方法（R13）
- **UI 层（MVVM + 声明式）**：
  - `ui/viewmodels/watchlist_view_model.py`：WatchlistState(frozen) + load/add/remove/is_in 命令
  - `ui/views/watchlist_view.py`：@ft.component 声明式，列表/空态/加载态，移除关注按钮
  - `ui/components/stock_detail_dialog.py`：新增 on_add_to_watchlist 回调
  - `ui/views/screener_view.py`：消费 WatchlistViewModel 实现加入关注流程 + toast 反馈
  - `ui/app_layout.py`：NavTabs 6→7（新增 WATCHLIST），_build_pages_stack + _build_nav_destinations
- **i18n**：`locales/zh_CN/strings.json` + `locales/en_US/strings.json` 各加 10 个 key
- **测试**：
  - `tests/unit/data/persistence/daos/test_watchlist_dao.py`：DAO 四方法行为（R4/R8/R12/R13）
  - `tests/unit/ui/viewmodels/test_watchlist_view_model.py`：VM 状态/命令/CancelledError 传播/frozen 契约
  - `tests/unit/ui/test_app_layout_contract.py`：契约测试更新为 7 视图

## 🧪 测试覆盖（必须全部勾选验证）
### 自动化测试
- [x] 单元测试：新增 DAO + VM + 契约测试，71 passed
- [ ] 集成测试：N/A（本 task 不涉及 Service 层链路）
- [ ] E2E测试(Playwright)：留待 CI 验证（tests/e2e/test_screener_flow.py）
- [x] 静态检查：ruff + pyright 无报错（pyright 仅 flet 导入环境警告，非代码错误）
- [ ] CI流水线全部通过：待 CI 验证（含 alembic downgrade base → upgrade head 回归）

### 手动验证
- [ ] 本地完整启动项目验证正向流程：留待 CI E2E
- [x] 边界场景、异常分支、并发场景验证：空态/重复加入(upsert)/移除不存在/DB未初始化 均覆盖
- [ ] 数据库迁移回滚验证：留待 CI（本地无 PG 服务，迁移文件已静态审查：链式/guard/downgrade/约束命名）
- [ ] UI变更附前后截图/GIF：N/A（声明式 UI，无截图工具）

## ⚠️ 风险与兼容说明
1. **破坏性变更**：无。watchlist 是新表，不影响现有表/接口。
2. **数据库迁移**：需执行 `alembic upgrade head`（0016 创建 watchlist 表）。downgrade 路径完整（drop_index + drop_table with guard）。
3. **定时调度/后台任务**：不影响。watchlist 是用户手动操作，无定时任务。
4. **安全相关**：无密钥/加密/输入校验变更。ts_code 经 DAO 参数化查询（R4 $1 占位符），无 SQL 注入风险。

## 📖 文档同步校验（适配文档检视规范）
- [ ] README.md：N/A（无功能/URL/配置变更）
- [ ] CLAUDE.md：N/A（无架构/单例/策略规范变更）
- [ ] CONTRIBUTING.md：N/A（无流程/安装/测试步骤变更）
- [ ] SECURITY.md：N/A
- [ ] docs/flet/ + man/：N/A（无 Flet API 约束变更）
- [x] CHANGELOG.md：由 release-please 自动生成

## ✅ 提交前自检清单（强制全部核对）
- [x] 代码符合 CLAUDE.md 架构红线 R1-R18、分层规范、单例约束
- [x] 无硬编码密钥、数据库密码、Token、隐私信息
- [x] 无废弃死代码、注释掉的临时调试代码
- [x] 所有新增分支、边界逻辑配套对应测试用例
- [x] 国际化文案统一使用I18n，无中文硬编码
- [x] 并发逻辑增加锁/信号量，无pd.options全局竞态污染
- [x] 数据库新增字段补充索引、非空、默认约束（ix_watchlist_ts_code + uq_watchlist_ts_code）
- [x] 已确认避免重复造轮子，优先复用了工程已有代码（BaseDao/ObservableViewModelMixin/use_viewmodel/EmptyState）
- [x] 本次修改引入的新问题已全部自查修复（review 反馈已修正：移除残缺详情对话框 + 删除重复 _set_state）
- [x] Ruff lint + format 已通过
- [x] Pyright 类型检查已通过
- [x] 测试断言避免弱断言
- [x] 所有 `# type: ignore` 均带 `[reason]` 注释（R3 红线）
- [x] 无 `# NOTE(lazy):` 标记

## 👀 评审重点提示
1. **Alembic 迁移 0016**：检查链式（0016←0015）、guard 幂等、downgrade 完整、约束命名与 models.py 一致
2. **NavTabs 6→7**：检查 app_layout.py 导航/路由/viewport 是否正确处理第 7 个视图
3. **CacheManager 注册**：检查 _DAO_REGISTRY + __init__ + _create_engine 循环更新 .engine（R13）
4. **MVVM 边界**：检查 View 不持业务状态、VM 不 import flet、VM 不调 page.update()
5. **screener_view 集成**：检查加入关注流程不影响现有选股流程

## 补充备注
- 本 PR 是 UX/URD 对齐批次（Plans.md）的 Task 4.2，Phase 4 收尾部分
- alembic 运行时验证未在本地执行（无 PG 服务），CI 会覆盖 downgrade base → upgrade head 回归
- review 反馈已修正：MAJOR 1（移除残缺详情对话框，采用方案 A）+ MAJOR 2（删除重复 _set_state）+ MINOR（is_in_watchlist 加 @log_async_operation + error_banner 颜色改 ERROR）
