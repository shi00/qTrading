# 已知架构技术债 (Known Technical Debt)

> 来源：从 CONTRIBUTING.md §已知架构技术债 迁移。CONTRIBUTING.md 原章节替换为指向本文的链接。

> Owner: 架构维护者
> 复核触发器: 排查深层问题 / upgrade 触发条件评估 / 红线违规频发 / 重构任务启动时

项目开发演进过程中产生了一些需要明确跟踪的技术债与设计限制，请在排查深层问题时参考。已解决事项（Windows 测试事件循环泄露、V0 兼容垫片删除、声明式迁移收官等）不再列入本表，仅在相关章节的活动规范中标注当前允许/禁止形态。

## 技术债清单

| 级别 | 问题描述 | 产生背景与现状 | 期望的最终解法 |
|------|---------|---------------|--------------|
| **P3** | **strategies/ 层 except Exception 已标记 NOTE(lazy)（ceiling 动态化），待评估统一走 classify_error** | strategies/ 层 except Exception 中 P0/P2 必修项已完成，剩余已合理日志的 except Exception 用 `# NOTE(lazy):` 标记保留。Phase 6.1 杠杆评估决策"不引入 `_handle_strategy_exception(e, context)` 统一入口"（YAGNI：`classify_error` 调用稀少；过度抽象：5 正交维度需 7 字段 context；多处差异化状态修改无法覆盖）。Phase 6.3 完成 ceiling 动态化（按方法分组描述，不再硬编码数值）。R9 合规修复已完成（`str(e)` → `DataSanitizer.sanitize_error(e)`）。 | 策略层重构或新增策略时统一走 `classify_error` + `classify_severity`。upgrade 触发条件：策略层重构时。评审决策：保持现状（NOTE(lazy) 标记已记录 upgrade 条件），强行接入 classify_error 会改变 system severity → raise 行为，违反 §1.4「不做无益重构」。 |
| **P3** | **utils/ 层 except Exception 已标记 NOTE(lazy)，待评估统一走 classify_error** | R3 场景遗漏检视发现：utils/ 层 except Exception（分布在 config_handler / exception_hooks / logger / singleton_registry / diagnostics 等）。已统一标记 `# NOTE(lazy):`（三要素齐全：简化内容 + ceiling + upgrade），`time_utils.py` 中一处候选标记已剔除（无业务消费方语义，调查时判定不适合标记）。0 处适合走 `classify_error`（均无业务消费方，属基础设施兜底或合理降级）。 | 后续 utils 层异常处理统一改造时重新评估是否引入 `classify_error`。upgrade 触发条件：utils 层异常处理统一改造时。 |
| **P3** | **红线自动化覆盖部分实现（R1/R4/R12/R13/R14/R15 已落地，R16 暂缓）** | CLAUDE.md §3.1 中 R1/R4/R12/R13/R14/R15/R16 标注「可自动化待实现」。R1 已由 import-linter 契约守护（4 条禁止导入契约）。R4/R12/R13/R14/R15 已由 `scripts/check_redlines.py` 实现，接入 `.pre-commit-config.yaml` `redline-check` hook（单元测试守护，数量见 CI 日志）。R16（UI 阻塞）暂缓：AST 扫描误报风险高，需更精确的事件处理器识别逻辑。 | R16 暂缓。upgrade 触发条件：R16 误报控制方案成熟或红线违规频发时重新评估。 |
| **P3** | **AIStrategyMixin（2173 行）高度内聚，context builder 拆分推迟评估** | `strategies/ai_mixin.py` 的 `AIStrategyMixin` 当前 2173 行（2026-07-18 复核：旧记录 1890 行已过时），承载 AI 策略 context 构建与决策调用，方法间共享状态密集、内聚度高。Phase 评审判定当前规模未触发可维护性红线（无跨策略复制粘贴、无重复 context 拼接逻辑），强行拆分会引入跨模块状态传递抽象，违反 §1.3「拒绝过度抽象」。文件增长单独不触发 upgrade 条件。 | 未来 AI 策略重构或新增 AI 策略类型时再评估拆分 context builder。验收标准：① context builder 拆为独立模块/类（如 `AIContextBuilder`）；② 拆分后 AI 策略行为不变（现有 AI 策略单测 + 集成测试全过）；③ 新增 context builder 单测覆盖核心分支。upgrade 触发条件：AI 策略重构或新增 AI 策略类型时。 |
| **P3** | **TushareClient（2116 行）capability probe 部分约 400 行可独立拆分** | `data/external/tushare_client.py` 的 `TushareClient` 当前 2116 行（2026-07-18 复核：旧记录 1852 行已过时），其中 capability probe（能力探测）部分约 400 行与主客户端职责正交，可独立拆分。当前未拆分原因：probe 完成后需通知上层刷新配置/缓存，缺少回调通知机制；强行拆分会引入循环依赖或全局状态。Phase 评审判定为推迟优化（YAGNI：当前 probe 部分稳定，无频繁变更）。文件增长单独不触发 upgrade 条件。 | capability probe 频繁变更或新增 probe 维度时拆分。验收标准：① probe 部分拆为独立模块/类（如 `TushareCapabilityProbe`）；② 引入回调通知机制（probe 完成/失败通知 `TushareClient` 上层）；③ 拆分后 `TushareClient` 单测全过；④ 新增 probe 模块单测覆盖各 API 探测分支。upgrade 触发条件：capability probe 频繁变更或新增 probe 维度时。 |
| **P3** | **CacheManager（1321 行）作为薄包装 facade 不主动拆分** | `data/cache/cache_manager.py` 的 `CacheManager` 当前 1321 行（2026-07-18 复核：旧记录 1109 行已过时，文件增长到 1321 行，仍属薄包装 facade，无业务逻辑泄漏迹象），但属薄包装 facade：方法多为对 DAO 实例/引擎生命周期的转发，无独立业务逻辑。Phase 评审判定按 §1.3「拒绝过度抽象」与 §4.3「单例模式」不主动拆分——拆分薄包装 facade 会增加间接调用层而无实质价值。 | 不主动拆分。仅在 facade 表面出现责任漂移（非缓存职责混入）或新增多个非薄包装逻辑时重新评估。验收标准：① 评估时确认仍是薄包装 facade（无业务逻辑泄漏到 facade）；② 如确有拆分必要，按职责拆分并保留 facade 兼容性（不破坏调用方）；③ `CacheManager` 单测 + `_reset_singleton` 隔离测试全过。upgrade 触发条件：facade 出现责任漂移或新增非薄包装逻辑时。 |
| **P3** | **R13 自动化增强（_create_engine/close 维度检查）为可选优化** | `scripts/check_redlines.py` 当前 R13 守护已落地：检查新增 DAO 在 `CacheManager.__init__` 中实例化。但 R13 红线全条款还要求「在 `_create_engine` 中更新 `.engine` 引用」与 close 路径正确释放，这两维度未做静态检查。Phase 评审判定为可选优化：① `_create_engine` 与 close 维度检查需 AST 解析方法体（复杂度高于 `__init__` 实例化检查）；② R5「僵尸引擎操作」运行时已有人工评审兜底；③ 当前未发生该维度漏报。 | 可选优化，不主动实施。新增 DAO 未在 `_create_engine` 更新 `.engine` 引用或未在 close 中正确释放导致 R5 问题时实施。验收标准：① `check_redlines.py` 增加 `_create_engine` / close 维度的 AST 检查；② 新增对应单元测试（正例 + 反例）；③ CI `redline-check` hook 通过。upgrade 触发条件：R13 这两维度出现漏报或 R5 僵尸引擎问题频发时。 |
| **P3** | **R16 AST 守卫维持暂缓，依赖 @log_async_operation 运行时检测** | CLAUDE.md §3.1 R16「UI 阻塞主循环」标注「可自动化待实现（AST 检查，暂缓：误报风险高）」。Phase 评审决策维持暂缓：① AST 扫描难以精确识别 Flet 事件处理器边界（onClick/on_change 等回调 vs 普通方法），误报率不可控；② 项目已有 `@log_async_operation(threshold_ms=PerfThreshold.XXX)` / `@track_performance()` 运行时检测慢操作告警，R16 漏报会被运行时告警捕获。本条与上表「红线自动化覆盖部分实现」条目互补：前者讲整体进度，本条专门记录 R16 维持暂缓的决策依据。 | 维持暂缓。误报控制方案成熟或 R16 运行时检测漏报频发时重新评估。验收标准：① AST 守卫接入 pre-commit `redline-check` hook；② 误报率 < 5%（在现有事件处理器样本上验证）；③ 不漏报已知的同步阻塞事件处理器；④ 新增对应单元测试（正例 + 反例 + 边界）。upgrade 触发条件：R16 误报控制方案成熟或运行时检测漏报频发时。 |
| **P3-WinE2E-Skip** | **Windows E2E 关键路径 skip（2026-07-25 复验后：6 用例已移除 skipif + 反向回滚条件，2 用例保留 skipif + 更新 reason）** | Windows 平台 Flet/Playwright CanvasKit textbox 渲染 + 向导状态隔离 + snackbar 时序 + select_dropdown 性能问题，原导致 8 个 E2E 用例在 Windows 下不稳定（30+ 分钟耗时或超时）。**2026-07-25 复验决策**（CI run 30138544395 + 30145028141，决策文档 `docs/debt/win-e2e-skip-revalidation/2026-07-25-decisions.md`）：**Group A（6 用例移除 skipif + 反向回滚条件）**：① `tests/e2e/test_onboarding_embedded.py::test_embedded_onboarding_zero_config_first_launch`；② `tests/e2e/test_onboarding_embedded.py::test_embedded_wizard_forward_then_back`；③ `tests/e2e/test_onboarding_embedded.py::test_embedded_db_info_message_displayed`；④ `tests/e2e/test_onboarding_embedded_real.py::test_embedded_real_onboarding_zero_config_first_launch`；⑤ `tests/e2e/test_onboarding_embedded_real.py::test_embedded_real_wizard_forward_then_back`；⑥ `tests/e2e/test_onboarding_embedded_real.py::test_embedded_real_db_info_message_displayed`。复验结果 11/11 通过率 100%（5 fake + 6 real；第 1 次 CI real-sidecar 4 用例全过、no-sidecar 被 cancelled；第 2 次 CI real-sidecar 3 用例全过、no-sidecar 3 用例全过）。**Group B（1 用例保留 skipif + 更新 reason）**：⑦ `tests/e2e/test_onboarding_wizard.py::test_wizard_db_validation_success`。复验结果 0/3 通过（第 2 次 CI FAILED，2 秒内失败）。根因：Windows CI 环境 CanvasKit 中文字体（NotoSansSC）从 `fonts.gstatic.com` 网络加载失败（`net::ERR_FAILED`）→ textbox a11y 节点未渲染到 DOM → `fill_textbox` 在 `wait_for(state="attached")` 阶段超时。根因分析归档 `docs/debt/win-e2e-skip-revalidation/2026-07-25-wizard-db-validation-failure-analysis.md`。**Group C（1 用例保留 skipif + 更新 reason）**：⑧ `tests/e2e/test_settings_flow.py::test_settings_log_level_switch`。复验结果 0/3 通过（第 2 次 CI 卡死 13+ 小时被手动取消）。根因：`select_dropdown` 暴力搜索引发 Chromium 渲染线程死锁（`match_keys` 缺少 "日志/log" 扩展分支致触发器定位失败）+ snackbar 浮层文本在 CanvasKit 下渲染机制不明致 `expect_text` 不命中。根因分析归档 `docs/debt/win-e2e-skip-revalidation/2026-07-25-settings-log-level-switch-hang-analysis.md`。**替代覆盖映射表**：Group A 6 用例（已移除 skipif）→ 无替代覆盖需求（移除 skipif 后 Windows CI `e2e-tests-windows` job 直接覆盖），无 gap；Group B `test_wizard_db_validation_success` → 替代覆盖 `tests/integration/test_onboarding_wizard_integration.py`（ViewModel/Service 层 DB 校验逻辑），**Gap**：未验证 Flet 声明式组件实际渲染输出（textbox a11y 节点渲染），**Gap 标注**：有意识接受（CanvasKit 渲染验证受限于 Windows CI 字体网络加载，超出测试基础设施能力）；Group C `test_settings_log_level_switch` → 替代覆盖 `tests/unit/ui/views/settings_tabs/test_system_tab.py` 的 `TestDoLogLevelChange`（覆盖 `_do_log_level_change` 成功/异常/CancelledError + state 验证），**Gap**：未验证 snackbar 在 UI 中实际出现 + 未验证 `select_dropdown` 在 CanvasKit 下的交互，**Gap 标注**：有意识接受（`select_dropdown` 在 CanvasKit 下的稳定性问题需要重构测试工具，超出技术债处理边界）。**反向回滚条件**（Group A 专用）：触发条件 = 移除 skipif 后 M=10 个 PR 内出现 flaky 失败（≥2 次）；触发动作 = 自动恢复 skipif + 升级债表等级（P3→P2）+ 重新评估根因 + 重新进入 Phase 2 复验流程；监控周期 = 从 PR 合并日起 30 天或 10 个 PR（以先到者为准）；监控方式 = CI 失败告警 + 定期 review CI 历史（每月一次）。**CI 矩阵**：`e2e-tests-windows` job（ci_cd.yml `e2e-tests-windows` step）跑 `tests/e2e/` 整个目录，移除 skipif 后 Group A 6 用例将在此 job 运行；`embedded-tests (linux)` job 跑 `test_onboarding_embedded_real.py` 全部 4 用例（Linux 不触发 skipif）；`embedded-tests (windows)` job 仅跑 4 个集成测试，不跑 E2E（E2E step `if: matrix.os == 'linux'`）；新增 `windows-skip-revalidation` job（ci_cd.yml `windows-skip-revalidation` step，3 matrix groups：no-sidecar / real-sidecar / settings-slow）用于本次复验，复验完成后保留 job 但设为 `workflow_dispatch` 手动触发。**E2E 模式渲染器**：`main.py` 强制 `web_renderer = ft.WebRenderer.CANVAS_KIT`（E2E 模式 Flet 以 Web 模式启动，CanvasKit 提供 Flutter Web 最完整的 a11y 语义树，是 Playwright 能与 Flet 控件交互的前提）。2026-07-25 复核：Flet 已升级到 0.86.2（`pyproject.toml` 的 `[project.dependencies]` 锁定 `flet==0.86.2` / `flet-desktop==0.86.2` / `flet-charts==0.86.2`，Flutter 3.44.7），但 0.86.0→0.86.2 升级未触及 CanvasKit 本身（engineRevision 未变化 `a10d8ac38de835021c8d2f920dbf50a920ccc030`），CanvasKit 行为预期不变。上游调研结论（2026-07-25，归档 `docs/debt/win-e2e-skip-revalidation/2026-07-25-upstream-research.md`）：① Flet GitHub issue tracker 无 CanvasKit Windows textbox 相关 issue（搜索 0 结果）；② Playwright 1.60（2026-05）无 snackbar 时序修复；③ Flutter 3.44.7 无 CanvasKit A11y 语义树修复。 | **Group A（6 用例已移除 skipif）**：反向回滚条件 = 移除 skipif 后 M=10 个 PR 内出现 flaky 失败（≥2 次）→ 自动恢复 skipif + 升级债表等级（P3→P2）+ 重新评估根因 + 重新进入 Phase 2 复验流程（Plans.md Task 2.4-2.8）。**Group B（`test_wizard_db_validation_success` 保留 skipif）**：upgrade 触发条件（任一满足时复验）：① CI 环境支持 `fonts.gstatic.com` 网络访问（GitHub Actions 网络策略变更）；② Flet 提供离线字体加载配置接口（如 CanvasKit fontRegistry 配置）；③ 项目主动预下载 NotoSansSC woff2 字体到 `tests/e2e/mock_assets/fonts/` 并配置 CanvasKit 离线加载。**Group C（`test_settings_log_level_switch` 保留 skipif）**：upgrade 触发条件（任一满足时复验）：① Flet 升级到提供更稳定 dropdown 渲染机制的版本（关注 engineRevision 变更）；② `select_dropdown` 重构为有硬性总 deadline（防止 Playwright 调用永久阻塞）；③ 为 "日志/log" 添加 `match_keys` 扩展分支（提升 JS 策略 0 + 策略 1-3 命中率）；④ snackbar 断言替换为更稳定的信号（如 dropdown 当前选中值）。**Reject 的替代方案**（2026-07-25 评审）：软件渲染（`--enable-software-rendering`）—— Flet desktop 不暴露切换接口，强行接入需 fork/patch Flet 内部，违反 §4.2 core 层隔离与 §1.3 拒绝过度抽象；UI Automation（xdotool/Windows UIA）—— 跨平台三套 OS 适配层 + 重写 FletPage 抽象，scope 远超技术债处理边界，且未消除 Flutter 自绘控件语义映射根因。 |
| **P3-Duplicate-Batch-Get** | **DAO batch get 同构模板跨多文件重复（2026-07-19 复核：13 方法/7 文件）** | `data/persistence/daos/` 下 7 个 DAO 文件含 13 个 `get_*_batch(ts_codes, as_of_date=None)` 同构方法：`financial_dao.py`（get_financial_reports_history_batch/get_fina_audit_batch/get_dividend_batch/get_pledge_stat_batch/get_fina_forecast_batch/get_fina_mainbz_batch）、`holder_dao.py`（get_top10_holders_batch/get_stk_holdernumber_batch）、`express_dao.py`（get_express_batch）、`pledge_detail_dao.py`（get_pledge_detail_batch）、`share_float_dao.py`（get_share_float_upcoming_batch）、`stk_holdertrade_dao.py`（get_stk_holdertrade_batch）、`top_inst_dao.py`（get_top_inst_batch）。模板结构高度同构：① 入参签名固定 `ts_codes: list[str], as_of_date=None`；② 早空返回 `pd.DataFrame()`；③ `try: chunked_in_query(self._read_db, sql_fn_or_str, ts_codes, params_fn=...)` 双分支（as_of_date 是否为 None）；④ `except asyncio.CancelledError: raise` + `except EngineDisposedError: raise` + `except Exception: logger.warning + return pd.DataFrame()` 兜底。强行抽象会引入 `BatchGetTemplate` 通用基类 + SQL 模板 DSL，违反 §1.3「拒绝过度抽象」（单实现的接口/永不变化的配置），故推迟。 | DAO 同构查询参数化提取为公共模板（如 `BaseDao._batch_get_with_as_of_date(sql_fn, ts_codes, as_of_date, log_prefix)`），消除双分支重复。验收标准：① 抽取后 13 方法行为不变（现有 DAO 单测全过）；② 新增模板单测覆盖 as_of_date 双分支 + 空入参 + 异常兜底。upgrade 触发条件：新增第 14 个同构 batch get 方法时提取公共模板。 |
| **P3-ScreenerVM-I18n-Get-Residual** | **`screener_view_model.py` 3 处 I18n.get 残留（VM 感知 locale 偏离，NOTE(lazy) 同主题已达 upgrade 阈值）** | `ui/viewmodels/screener_view_model.py` 含 3 处 I18n.get 残留（违反 CLAUDE.md §3.2「VM 不感知 locale；VM 只产出 i18n key 与 params，View 按当前 locale 渲染」）：① `ScreenerViewModel.get_strategy_desc` 调 `I18n.get(st.desc_key)` 返回翻译字符串供 `update_strategy_desc` 拼接 `state.strategy_desc`（str 字段）；② `ScreenerViewModel.update_strategy_desc` 调 `I18n.get('strategy_missing_apis')` 拼入 `warning_suffix` 形成翻译字符串 desc；③ `ScreenerViewModel.set_history_viewing_status` 的 `label` 入参为 View 层 `translate_strategy_name` 已翻译字符串（VM 不应接收已翻译值），存入 `state.status_message.params["label"]`。3 处均带 `# NOTE(lazy)` 或 `# I18N_GET_ALLOWED` 标注，ceiling 均为「VM state 非 Message 结构 / 未引入 strategy_name_key 机制」，upgrade 均为「R.3 strategy_name 标准化启动时统一改造」。同主题 NOTE(lazy) 已达 3 处（≥3 触发升级为债表条目，按 CLAUDE.md §3.3 规则），本次补登记。 | R.3 strategy_name 标准化启动时统一改造：① `state.strategy_desc` 改为 `Message` 结构（desc_key + params），View 渲染时翻译；② `strategy_obj.get_dynamic_description(params)` 返回值改为 i18n key（而非翻译字符串）；③ `translate_strategy_name` 迁入 VM 或引入 `strategy_name_key` 机制，`label` 改为传 raw strategy_name + View 渲染时翻译。验收标准：① 3 处 I18n.get 残留消除（VM 内无 I18n.get 调用）；② 现有 screener_view_model 单测全过；③ 新增 locale 切换自动刷新单测。upgrade 触发条件：R.3 strategy_name 标准化启动时统一改造。 |
| **P3-Config-Test-DeepCopy-Coverage** | **`test_returns_independent_dict` 测试嵌套隔离覆盖较浅** | F3 修复 `ConfigHandler.load_config` 返回 `copy.deepcopy` 后，`tests/unit/test_config_models.py` 的 `test_returns_independent_dict` 仅验证顶层 dict 与一层嵌套（`"nested": {"key": "value"}`）的独立性。对更深嵌套（如 `llm_provider_credentials` 内的 provider dict）未覆盖。本次检视判定不立即增强：① `copy.deepcopy` 是递归实现，浅层验证已能证明调用路径正确；② 项目无 ≥3 层嵌套的配置项；③ 增强测试无实质价值。相关文件：tests/unit/test_config_models.py 的 `test_returns_independent_dict`。 | 若未来引入深层嵌套配置（如 `llm_provider_credentials.provider.models[].metadata`），补充针对性测试。验收标准：① 新增 ≥3 层嵌套配置的独立性测试；② 修改嵌套子 dict 不影响原配置。upgrade 触发条件：配置结构引入 ≥3 层嵌套时。 |
| **P3-Config-SaveLLM-Empty-Path-Untested** | **`save_llm_config` 清空路径（models=[]/api_key=""）缺少专项返回值测试** | F1 修复 `ConfigHandler.save_llm_config` 传播 `save_config` 返回值后，`tests/unit/utils/test_config_handler.py` 的 `TestConfigReviewFixes` 覆盖了正常写入与 `save_config` 失败路径，但未覆盖"清空 models 列表"与"清空 api_key"等边界路径。本次检视判定不立即增强：① 清空路径与正常写入共用同一 `save_config(config_update)` 调用，无独立分支；② 返回值传播逻辑已被现有测试充分覆盖；③ 补充清空路径测试不会发现新缺陷。相关文件：tests/unit/utils/test_config_handler.py 的 `TestConfigReviewFixes` 类。 | 若 `save_llm_config` 未来引入清空路径的独立分支（如清空时跳过 keyring 同步），补充专项测试。验收标准：① 清空路径有专项返回值断言；② 清空路径的 keyring/加密 fallback 行为被覆盖。upgrade 触发条件：`save_llm_config` 清空路径引入独立分支时。 |
| **P3-Rust-Coverage-D38** | **Rust sidecar unit 覆盖率 61.15% 未达 §17.0 门禁 85%（D38）** | pg_plan.md §17.0 要求 Rust unit 行覆盖率 ≥85%，Phase 1 合并时未设门禁。2026-07-23 实测 TOTAL 61.15%（`run.rs` 10.22%/`maint.rs` 45.20%/`pgbin.rs` 45.03% 测试不足，三文件占 77% 缺失行）。`sidecar.yml` lint-test job 当前 advisory 模式（无 `--fail-under-lines 85`），NOTE(lazy) 标记 ceiling 85% + upgrade 条件。D38 决策：P0-5 拆为独立 PR 推进，不阻塞 pg-plan-review 主 PR。补齐至 85% 需覆盖 ~758 行（run.rs supervisor 难以单测，需 mock postgresql_embedded 或重构可测路径）。相关文件：`sidecars/qtrading-pg-sidecar/src/run.rs`/`maint.rs`/`pgbin.rs`、`.github/workflows/sidecar.yml`、`reviews/pg_plan.md` §22 D38。 | 独立 PR 补充 Rust unit 测试至 ≥85% 后：① 恢复 `sidecar.yml` 的 `--fail-under-lines 85`；② 移除 NOTE(lazy) 注释；③ 更新 pg_plan.md §22 D38 状态。验收标准：① `cargo llvm-cov --fail-under-lines 85` 通过；② run.rs/maint.rs/pgbin.rs 重点分支有单测覆盖；③ CI sidecar.yml 门禁恢复 enforce。upgrade 触发条件：独立 PR 推进时（不阻塞当前主 PR）。 |
| **P3-Fi01-Fi06-Rust-Injection-Missing** | **§17.6 失败注入场景 #1（initdb exit 11）与 #6（磁盘满 exit 15）缺 Rust 集成测试** | pg_plan.md §17.6 失败注入测试矩阵 33 场景中，`failure_injection.rs` 现覆盖 9 场景（#3/#4/#8/#9/#23/#24/#26/#27/#31），#1 与 #6 缺失。根因分析（2026-07-23，基于代码现场）：① **#1 initdb exit 11**——`src/setup.rs` 的 `INITDB_FAILED` 触发条件为 initdb exec 失败或 exit code 非 0，无 env override 钩子可注入失败；唯一可行路径是「写入 corrupted install dir（无 initdb binary）」但在 `setup` 已缓存 install dir 的复用机制下会被重做 setup 而非触发 INITDB_FAILED；② **#6 磁盘满 exit 15**——`src/preflight.rs` 的 `PreflightFailure::DiskSpace` 触发条件为 `free_space() < MIN_FREE_BYTES`，仅有 `QTRADING_PG_SIDECAR_FORCE_FS_KIND` env override（只影响 fs_kind 判定，不触发 DiskSpace 失败）；Linux chmod 0500 只读目录可触发 `NotWritable`（已被 #26 覆盖），但与 #6「磁盘满」语义不同。新增 env override 钩子需修改生产代码（如 `QTRADING_PG_SIDECAR_FORCE_INITDB_FAIL` / `QTRADING_PG_SIDECAR_FORCE_DISK_FULL`），违反 §1.3「极简原则 / 拒绝过度抽象」与 §1.4「不做无益重构」（仅为测试增加生产代码复杂度）。母计划 `pg-plan-landing-review-fix.md` 风险缓解方案：「若无可靠 mock 路径，记录为 P2 技术债（NOTE(lazy) 标注 ceiling + upgrade）」。相关文件：`sidecars/qtrading-pg-sidecar/tests/failure_injection.rs`、`src/setup.rs`（INITDB_FAILED exit 11）、`src/preflight.rs`（PreflightFailure::DiskSpace exit 15）、`reviews/pg_plan.md` §17.6。 | 引入 Rust mock 框架（如 `mockall` trait 注入）重构 initdb/preflight 调用点为可 mock trait 后补齐 #1/#6 集成测试；或在不污染生产代码的前提下新增 env override 钩子（仅在 cfg(test) 测试构建可见）。验收标准：① `failure_injection.rs` 新增 `test_inject_01_initdb_failure` 与 `test_inject_06_disk_full`；② 触发 exit 11 / exit 15 与对应 stderr 输出断言通过；③ 不在生产代码引入 test-only 分支或新增 env override 钩子（除非通过 trait 抽象合理化）。upgrade 触发条件：① sidecar 引入 mock 框架重构可测路径时；② 或 initdb/preflight 模块因其他原因重构时顺手补齐。 |
| **P3-Fi25-Rust-Injection-Missing** | **§17.6 失败注入场景 #25（数据页 checksum 错误）缺 Rust 集成测试** | pg_plan.md §17.6 失败注入测试矩阵场景 #25：篡改测试表数据页后查询 → PostgreSQL 报 `invalid page in block` → sidecar stdout warning 事件上报 → UI 提示备份恢复（§13.7.38）。`failure_injection.rs` 现覆盖 10 场景（#3/#4/#8/#9/#23/#24/#26/#27/#28/#31），#25 缺失。根因分析（2026-07-23）：① **需协议扩展**——当前 sidecar stdout 协议仅 `ready.v1` / `event.warning.v1` / `event.exit.v1` / `status.v1` / `doctor.v1` / `version.v1` 六类 schema，运行期 PostgreSQL 日志监听 + warning 事件 emission 尚未实现（`run.rs` 的 `supervise` 循环无 postgres stderr/log 关键字扫描）；② **需运行期日志监控**——sidecar 需新增 daemon thread/task 持续读取 PostgreSQL `log_min_messages=warning` 输出，匹配 `invalid page in block` 关键字后经 `protocol::print_json_line(&EventJson::warning(...))` 上报 stdout；③ **需 UI 集成**——Python 侧 `_stdout_reader_task` 需解析 `event.warning` 事件并经 ViewModel state 通知 UI 显示"检测到数据页损坏，建议尽快备份并从最近备份恢复"。三端协同改造 scope 远超 P2 修复边界，母计划策略"对影响正确性的 P2 修复（如 #25/#28）"中 #25 归类为"需协议扩展的设计偏差"，按 §1.3 YAGNI 推迟。**注**：#28（restore 中断残留）已在本次 pg-plan-review 修复（doctor 新增 `scan_residuals` + `restore_residuals` / `dump_partials` 字段 + 集成测试 `test_inject_28_restore_interruption_residual`）。相关文件：`sidecars/qtrading-pg-sidecar/src/run.rs`（supervise 循环）、`src/protocol.rs`（EventJson::warning）、`src/maint.rs`（doctor scan_residuals 已实现）、`data/persistence/embedded_postgres/service.py`（_stdout_reader_task）、`reviews/pg_plan.md` §17.6 #25 / §13.7.38。 | ① sidecar `run.rs` supervise 循环新增 postgres 日志监听 task（持续读 postgres stderr/log file），匹配 `invalid page` / `checksum failure` 关键字后经 `protocol::print_json_line(&EventJson::warning("data_page_corruption", ...))` 上报 stdout；② Python `_stdout_reader_task` 解析 `event.warning` 事件并经 ViewModel state 通知 UI；③ UI 显示数据页损坏警告 + 引导备份恢复；④ `failure_injection.rs` 新增 `test_inject_25_data_page_checksum_error` 集成测试（篡改测试表数据页 → 查询 → 验证 sidecar stdout warning 事件）。验收标准：① sidecar 监听 postgres 日志并上报 `data_page_corruption` warning 事件；② Python + UI 正确显示警告；③ 集成测试覆盖 #25 场景；④ 不破坏现有 stdout 协议向后兼容。upgrade 触发条件：① sidecar 引入运行期 postgres 日志监控需求时；② 或数据页损坏问题在生产环境报告时。 |
| **P3-E2E-Sidecar-Ready-Path** | **E2E 未覆盖真实 sidecar ready→migration→主界面路径** | Stage 4 View 2 检视发现：`tests/e2e/` 现有 onboarding/navigation/status 测试用 `fake_sidecar` fixture 模拟 sidecar stdout，未覆盖真实 sidecar binary 的 ready 事件 → Python 解析 → DB 迁移 → 主界面渲染的端到端路径。根因：真实 sidecar binary 在 E2E 环境需下载 PostgreSQL binaries（约 30MB）+ initdb（约 5-10s），E2E 超时预算紧张；且 Windows E2E 已有 CanvasKit headless 限制（P3-E2E-Headless）。当前用 `fake_sidecar` + 集成测试 `tests/integration/test_embedded_postgres_service.py` 分别覆盖 Python 解析路径与 sidecar Rust 单元/集成测试，端到端路径存在覆盖 gap。相关文件：`tests/e2e/conftest.py`（fake_sidecar fixture）、`tests/e2e/test_onboarding.py`、`tests/integration/test_embedded_postgres_service.py`。 | 引入"真实 sidecar smoke E2E"用例（标记 `@pytest.mark.network` + `@pytest.mark.slow`），仅在 release tag 触发的 CI job 运行（非 PR 必跑门禁），验证真实 sidecar ready → migration → 主界面路径。验收标准：① 新增 E2E 用例覆盖真实 sidecar 路径；② 仅在 release tag 触发，不阻塞 PR；③ 用例失败有清晰诊断输出（sidecar 日志 + Python 解析日志）。upgrade 触发条件：① E2E 超时预算优化后（如 sidecar 预下载 PostgreSQL binaries 缓存）；② 或 release 流程要求端到端 sidecar 验证时。 |
| **P3-CI-Installer-Smoke-Trigger** | **installer smoke 测试仅在 release tag 触发** | Stage 4 View 3 检视发现：`installer.iss` 的 smoke 测试（PowerShell 语法检查 + ISCC 编译）仅在 release tag 触发的 CI job 运行，PR 阶段不验证 installer 完整性。若 PR 修改 `installer.iss`（如新增 sidecar 进程检测逻辑 P1-11），PR CI 不跑 installer smoke，缺陷可能延迟到 release tag 阶段才暴露。当前 `ci_cd.yml` 的 `paths` 过滤器含 `'installer.iss'` 但仅触发 `e2e-tests-windows` job，无独立 installer smoke step。相关文件：`.github/workflows/ci_cd.yml`、`installer.iss`、`scripts/check_installer.ps1`（若存在）。 | 在 `ci_cd.yml` 新增 `installer-smoke` job（或在 `e2e-tests-windows` job 内新增 step），当 PR 修改 `installer.iss` 或 `scripts/*installer*` 时触发 ISCC 编译 smoke 测试（不生成 release artifact，仅验证编译通过）。验收标准：① PR 修改 installer.iss 时触发 smoke；② 编译失败时 CI fail；③ 不影响 release tag 触发的完整 release 流程。upgrade 触发条件：① installer.iss 在 PR 阶段频繁修改导致 release tag 阶段才发现缺陷时；② 或引入第三方 installer 工具时。 |
| **P3-CI-Installer-PowerShell-Syntax-Only** | **installer smoke 仅 PowerShell 语法检查，未验证 ISCC 编译** | Stage 4 View 3 检视发现：当前 installer smoke 测试（若存在）仅做 PowerShell 语法检查（`Test-Path`/`Get-Content` 等 cmdlet 语法），未验证 Inno Setup Compiler (ISCC) 能否成功编译 `installer.iss` 生成 installer.exe。ISCC 编译需要 Windows runner + ISCC 安装，PR 阶段未触发，仅在 release tag 阶段验证。本次 P1-11 修复在 `installer.iss` 新增 `InitializeSetup` sidecar 进程检测逻辑，需 ISCC 编译验证语法正确性，但 PR 阶段未跑 ISCC。相关文件：`installer.iss`、`.github/workflows/ci_cd.yml`、`.github/workflows/release.yml`（若存在）。 | 与 P3-CI-Installer-Smoke-Trigger 合并解决：在 PR 阶段触发 ISCC 编译 smoke 测试（Windows runner + ISCC 安装），验证 `installer.iss` 编译通过。验收标准：① PR 修改 installer.iss 时触发 ISCC 编译；② 编译失败时 CI fail；③ 不生成 release artifact（仅验证编译）。upgrade 触发条件：与 P3-CI-Installer-Smoke-Trigger 一致。 |
| **P3-Scheduler-OnJobError-ExcInfo-Guard** | **`scheduler_service._on_job_error` 是文件内唯一不带 `exc_info=True` 的 error log；若未来补加需同步 sanitize traceback** | M2 模块化检视修复 `utils/scheduler_service.py:283` 的 R9 违反（`event.exception` → `DataSanitizer.sanitize_error(event.exception)`）时发现：该行是 `scheduler_service.py` 内 7 处 `logger.error` 中唯一不带 `exc_info=True` 的（其他 6 处行 97/204/257/330/466/594 均带）。本次修复仅脱敏异常消息（`sanitize_error` 默认 `show_traceback=False`），未触及 `exc_info` 设置，符合 §1.4「微创修改」。但若未来有人为行 283 补加 `exc_info=True` 以记录完整 stack trace，stack trace 中的 frame 局部变量可能含敏感字段（DB 连接串、token、文件路径），需同步改为 `DataSanitizer.sanitize_error(event.exception, show_traceback=True)` 才能脱敏 traceback。相关文件：`utils/scheduler_service.py:283`、`utils/sanitizers.py`（`DataSanitizer.sanitize_error` 签名）。 | 为行 283 补加 `exc_info=True` 时同步改为 `DataSanitizer.sanitize_error(event.exception, show_traceback=True)`。验收标准：① stack trace 经 sanitize_error 脱敏后无明文 token/路径/凭证；② `test_on_job_error_other` 单测覆盖含敏感字段的异常场景（如 `RuntimeError("token=sk-secret123")`）并断言输出脱敏。upgrade 触发条件：① 为行 283 补加 `exc_info=True` 时；② 或 audit R9 时发现其他 error log 缺少 traceback 脱敏时。 |
| **P3-App-ExcInfo-Traceback-Path-Leak** | **`exc_info=True` 的 traceback 文件路径未经 sanitize_error 脱敏（合理设计决策，供未来评估）** | M3 模块化检视发现：`app/error_logging.py:40`（`log_exception_with_severity`）与 `app/bootstrap.py` 8 处 `_log(..., exc_info=True)` 调用时，logging 模块从 `sys.exc_info()` 提取 traceback 附加到日志记录，traceback 含文件绝对路径（如 `D:\workspace\qTrading\app\bootstrap.py`）。`DataSanitizer.sanitize_error` 默认 `show_traceback=False`（已确认 `utils/sanitizers.py:143`），只处理 exception 消息字符串，不处理 `exc_info` 附加的 traceback。检视结论：文件路径泄露不是 R9 红线违规（R9 定义为"明文 Token/API Key/密码/个人信息"，文件路径不属于这些类别）；`exc_info=True` 是调试必需的（traceback 对问题定位至关重要），当前实现是"调试可见性"与"信息泄露风险"之间的合理权衡。本次 M3 修复了 `detail=str(e)` → `DataSanitizer.sanitize_error(e)`（FIND-01），但未触及 `exc_info` 的 traceback 路径。相关文件：`app/error_logging.py:40`、`app/bootstrap.py` L64/107/184/216/272/302/321、`app/startup_controller.py` L111/187、`utils/sanitizers.py:143`。 | 如需统一脱敏 traceback 路径：① 自定义 logging Formatter（在日志输出层统一处理 traceback 路径脱敏）；② 或在 `sanitize_error` 中支持 `exc_info` 参数自动提取并脱敏 traceback。验收标准：① traceback 中的文件路径经脱敏后为 `<PATH>`；② 现有 `test_error_logging.py` 全过；③ 新增 traceback 路径脱敏单测。upgrade 触发条件：① 安全审计要求脱敏文件路径时；② 或引入自定义 logging Formatter 时。 |
| **P3-App-StartupPartialFailure-Leak** | **`initialize_services` 启动部分失败时已启动服务资源暂时泄漏（跨模块延后）** | M3 模块化检视发现：`app/bootstrap.py:125-127` 的 `SchedulerService().start()` / `await NewsSubscriptionService().start()` / `await MarketDataService().start()` 三个 start 调用无 try/except 保护。若 `NewsSubscriptionService.start()` 或 `MarketDataService.start()` 抛异常，已启动的 SchedulerService 继续运行。异常传播到 `StartupController._init_services` except 块（startup_controller.py L98），转为 INIT_FAILED 状态。影响：① 用户看到 INIT_FAILED 后若点击 Skip（startup_controller.py L161-165 `skip()`），已启动的服务继续运行直到最终关机；② 最终关机时 `ShutdownCoordinator` Step 1（`utils/shutdown.py:365-387`）会停止所有服务，暂时泄漏可接受；③ `reconfigure()` 仅调用 `cache_manager.close()`，不停止已启动服务。本次 M3 未修复原因：修复需追踪哪些服务已启动并调用各自的 stop 方法（SchedulerService.stop() 同步 / NewsSubscriptionService.stop() async / MarketDataService.stop() async，签名不同），跨 3 个服务模块，需配套测试，违反 §1.3「极简」与 §1.4「微创修改」原则。相关文件：`app/bootstrap.py:125-127`、`app/startup_controller.py:98-114,161-165`、`utils/shutdown.py:365-387`。 | 在 `bootstrap.py:125-127` 外层加 try/except，失败时调用已启动服务的 stop/cleanup。验收标准：① 追踪已启动服务（如用 flag 或 list）；② 异常时按逆序调用各服务 stop 方法；③ 新增 `test_bootstrap.py::TestInitializeServicesStartFailures` 覆盖部分失败场景；④ 现有测试全过。upgrade 触发条件：① 用户报告 Skip 后后台服务干扰；② 或 3 个服务模块重构统一 stop 签名时。 |
| **P3-App-SyncFunctions-In-Async-Bootstrap** | **`initialize_services` 中同步函数（preload_aliases/validate_failover/validate_strategy_tier）阻塞事件循环（合理设计决策）** | M3 模块化检视发现：`app/bootstrap.py:77`（`MetaDataManager.preload_aliases()`）、L131（`_validate_failover_credentials()`）、L134（`_validate_strategy_tier_coverage()`）在 async `initialize_services` 中同步调用，阻塞事件循环。检视结论：① `preload_aliases` 是纯内存操作（遍历 TABLE_DEFINITIONS + I18n.get 翻译），毫秒级；② `_validate_failover_credentials` 首次读配置文件（`config_handler.py:285-291` `open(CONFIG_FILE)`），后续走 `_config_cache`，毫秒级；③ `_validate_strategy_tier_coverage` 调用 `StrategyManager().strategies.keys()` + `validate_strategy_tier_coverage()`，纯内存操作。R16 红线聚焦"Flet 事件处理器中的同步阻塞场景"（如点击按钮后同步等待网络响应），启动期一次性同步操作（毫秒级内存/配置文件操作）不影响用户体验，不适用 R16。修复（用 ThreadPoolManager 包装）会增加复杂度且收益不显著，违反 §1.3「极简」。相关文件：`app/bootstrap.py:77,131,134`、`data/persistence/metadata_manager.py:14-23`、`utils/config_handler.py:279-291`。 | 不主动修复。若未来 `preload_aliases` / `validate_failover_credentials` / `validate_strategy_tier_coverage` 引入网络 IO 或耗时 CPU 操作（如 >100ms），改用 `ThreadPoolManager().run_async(TaskType.IO, ...)` 提交。验收标准：① 改造后启动期 UI 无阻塞；② 现有 `test_bootstrap.py` 全过。upgrade 触发条件：① 这些函数引入网络/磁盘 IO；② 或启动期 UI 响应延迟被用户感知时。 |
| **P3-M4-DbMigrator-OrphanHeal-SingleChain-Assumption** | **`_heal_orphaned_revision` 假设严格线性单链迁移（已文档化设计限制）** | M4 模块化检视发现：`data/persistence/db_migrator.py:149-153` 的 `_heal_orphaned_revision` 注释明确「严格线性单链迁移策略」，「未来引入多分支需重新评估」。当前 15 个迁移（0001~0015）均为单链，假设成立。未来若引入 Alembic 多分支（Multiple Heads），「直接拨回 head」逻辑可能导致 schema 漂移。属于已文档化的设计限制，本次 M4 仅补登记到债表以跨检视周期追溯。相关文件：`data/persistence/db_migrator.py:149-153`、`alembic/versions/0001~0015`。 | 引入 Alembic 多分支时重新评估 `_heal_orphaned_revision` 逻辑：① 检测 Multiple Heads 并拒绝自动 heal；② 或实现多分支 merge 逻辑。验收标准：① 多分支场景下 `_heal_orphaned_revision` 不再「直接拨回 head」；② 新增多分支场景单测。upgrade 触发条件：引入 Alembic 多分支（Multiple Heads）时。 |
| **P3-M5-ClassifyError-System-Gap** | **`data/cache/cache_manager.py` + `data/persistence/daos/base_dao.py` 均未接入 `classify_error`/`classify_severity`（系统性缺失）** | M5 模块化检视发现：CLAUDE.md §3.2 强制要求「错误处理必须使用 `classify_error()` + `classify_severity()` 进行分类，并按严重度选择日志级别」，但 `cache_manager.py` 全部 except 块（约 17 处）与 `base_dao.py` 全部 except 块（约 15 处）均未接入 `utils.error_classifier.classify_error` / `classify_severity`，而是手写 `logger.error/warning/debug` + 字符串匹配识别连接类错误。M5 修复仅做 R9 脱敏（`safe_error(e)`）+ R5 守卫（`EngineDisposedError`），未接入错误分类体系。未修复原因：`base_dao.py` 也未接入，是 DAO 层系统性缺失，修复需跨 `data/cache/` + `data/persistence/daos/` 两层，且需配套测试覆盖每个 except 路径的分类断言，违反 §1.4「微创修改」。相关文件：`data/cache/cache_manager.py`、`data/persistence/daos/base_dao.py`、`utils/error_classifier.py`。 | DAO 层错误处理统一改造时接入 `classify_error` + `classify_severity`：① `cache_manager.py` 与 `base_dao.py` 所有 except 块改用分类器决定日志级别；② 移除手写字符串匹配（`["no active connection", ...]`）改为分类器枚举；③ 配套单测覆盖每个 except 路径的分类断言。验收标准：① DAO 层全量 except 块接入分类器；② 现有 `test_cache_manager*.py` + `test_base_dao*.py` 全过；③ 新增分类断言单测。upgrade 触发条件：① DAO 层错误处理统一改造时；② 或 `error_classifier` 重构时。 |
| **P3-M5-HealthCheck-CancelEvent-Gap** | **`check_comprehensive_health` 未每 2 秒检查 `cancel_event`（取消响应延迟）** | M5 模块化检视发现：`data/cache/cache_manager.py` 的 `check_comprehensive_health`（L590-839）内嵌 `_check_single_table` 多次 `await conn.execute(...)` 之间无显式 `cancel_event` 检查。`gather_return_exceptions_propagating_cancel` 会传播 `CancelledError`，但 asyncio.gather 在协程阻塞于 DB IO 时取消传播依赖驱动响应。project_memory 明确要求「Long-running operations must check cancel_event every 2 seconds」。监控表数量较多（monitored_tables 可能 20+）时整体可能超过 30 秒，用户触发关闭时取消传播延迟。M5 未修复原因：需重构 `_check_single_table` 循环结构插入 cancel 检查点，属行为变更需配套并发测试，违反 §1.4「微创修改」。相关文件：`data/cache/cache_manager.py:590-839`。 | 在 `_check_single_table` 循环关键点（每次 `conn.execute` 后）检查 `cancel_event`，或在 `check_comprehensive_health` 主循环按表批次检查。验收标准：① 取消响应 ≤2s；② 现有 `test_cache_manager*.py` 健康检查测试全过；③ 新增取消响应延迟单测。upgrade 触发条件：① 健康检查耗时成为用户感知瓶颈；② 或取消响应延迟被用户报告时。 |
| **P3-M5-LogAsyncOperation-Coverage** | **`@log_async_operation` 仅挂载 `cache_manager.py` 5 个方法 + `base_dao.py` 2 个方法（系统性覆盖不足）** | M5 模块化检视发现：CLAUDE.md §3.2 强制要求「涉及外部 IO (Tushare/LiteLLM/DB) 的方法必须挂 `@log_async_operation(threshold_ms=PerfThreshold.XXX)` 或 `@track_performance()`」。`cache_manager.py` 仅 `write_db`/`init_db`/`hard_reset`/`clear_all_cache`/`check_comprehensive_health` 5 个方法挂载；`base_dao.py` 仅 `_read_db`/`_read_db_select` 2 个方法挂载，`_write_db`/`_save_upsert` 未挂载（仅有手写 `perf_counter` 测量）。`cache_manager.read_db` 直接 SQL 路径（L275-277）未挂载。M5 未修复原因：`base_dao.py` 也未全覆盖，是 DAO 层系统性问题，修复需跨层评估，违反 §1.4「微创修改」。相关文件：`data/cache/cache_manager.py`、`data/persistence/daos/base_dao.py`、`utils/log_decorators.py`。 | DAO 层性能监控统一改造时：① `base_dao._write_db`/`_save_upsert` 挂载 `@log_async_operation`；② `cache_manager.read_db` 直接 SQL 路径挂载；③ 评估 80+ 代理方法是否需挂载（若 DAO 层已覆盖则否）。验收标准：① DAO 层核心方法全挂载；② 现有测试全过；③ 新增慢操作告警单测。upgrade 触发条件：① DAO 层性能监控统一改造时；② 或慢操作告警漏报频发时。 |
| **P3-M4-AppStateService-BroadExcept** | **`app_state_service.py:21,38` broad except 未接入 classify_error（合理降级，一致性建议）** | M4 模块化检视发现：`data/persistence/app_state_service.py:21,38` 共 2 处 `except Exception as e:` 吞没异常返回默认值。app state 是非关键路径（用户偏好/窗口位置等），吞没降级返回默认值是合理设计。但未接入 `classify_error`，与 #M4-002（review_manager 已接入 classify_error）一致性不符。本次 M4 未修复原因：① app state 非关键路径，容错优先；② 修复需引入 `classify_error` + `classify_severity` + system 级 raise 传播，改变现有降级语义，违反 §1.4「不做无益重构」。相关文件：`data/persistence/app_state_service.py:21,38`。 | 与 #M4-002 一致性评估：① 若 app state 升级为关键路径，接入 `classify_error` + `classify_severity`，system 级 raise 传播；② 或维持现状（合理降级）。验收标准：① 若改造，现有 `test_app_state_service.py` 全过；② 新增 system 级异常 raise 传播单测。upgrade 触发条件：① app state 重构为关键路径时；② 或与 #M4-002 一并修复时。 |
| **P3-M6-NewsFetcher-I18n-Get-In-Data** | **`news_fetcher.py` 多处 `I18n.get` 调用违反 i18n 状态驱动规则（data 层不应感知 locale）** | M6 模块化检视发现：`data/external/news_fetcher.py:13` 顶部 `from core.i18n import I18n`，并在 L297（`title_str = I18n.get("news_no_title")`）等多处直接调用 `I18n.get` 获取翻译字符串。CLAUDE.md §3.2「UI 模型（强制）」明确「VM 只产出 i18n key，View 按当前 locale 渲染」，data 层更不应感知 locale。`news_no_title` 等翻译字符串直接拼入返回给上层的数据结构（`news_list.append({"title": title_str, ...})`），导致 data 层输出随 locale 变化，破坏数据层与表现层隔离。M6 未修复原因：修复需 data 层返回 i18n key（如 `news_no_title_key`）由 VM/View 翻译，跨 `data/external/` + `ui/viewmodels/` 两层，违反 §1.4「微创修改」。相关文件：`data/external/news_fetcher.py:13,297`。 | data 层 i18n 改造时：① `news_fetcher.py` 移除 `from core.i18n import I18n`；② 返回值中的翻译字符串改为 i18n key（如 `"title": "news_no_title"`）；③ VM/View 层在渲染时翻译。验收标准：① `news_fetcher.py` 无 `I18n.get` 调用；② 现有 `test_news_fetcher.py` 全过；③ 新增 locale 切换自动刷新单测。upgrade 触发条件：data 层 i18n 改造或 VM 重构时。 |
| **P3-M6-NewsFetcher-Lazy-ProxyManager-Import** | **`news_fetcher.py:409` 函数内部 import ProxyManager（合理避免循环依赖，建议保留）** | M6 模块化检视发现：`data/external/news_fetcher.py:409` 在 `get_us_major_moves` 内部 `from utils.proxy_manager import ProxyManager`，属函数内部 import。一般而言函数内部 import 是反模式，但此处合理：`utils/proxy_manager.py` 可能间接依赖 `data/external/`（通过配置或单例注册链），顶部 import 会形成循环依赖。M6 未修复原因：修复需重构 `proxy_manager.py` 的依赖方向或提取 proxy 配置为独立模块，跨 `utils/` + `data/external/` 两层，违反 §1.4「微创修改」。相关文件：`data/external/news_fetcher.py:409`、`utils/proxy_manager.py`。 | 若 `proxy_manager.py` 重构解除循环依赖，将 import 移至模块顶部。验收标准：① 顶部 import 无循环依赖；② 现有 `test_news_fetcher.py` 全过。upgrade 触发条件：`proxy_manager.py` 重构或循环依赖被解除时。 |
| **P3-M6-NewsFetcher-ClassifyError-Gap** | **`news_fetcher.py` 多处 except 块未接入 `classify_error`/`classify_severity`（系统性缺失）** | M6 模块化检视发现：CLAUDE.md §3.2 强制要求「错误处理必须使用 `classify_error()` + `classify_severity()` 进行分类」，但 `news_fetcher.py` 全部 except 块（约 12 处）均未接入 `utils.error_classifier`，而是手写 `logger.error/warning/debug` + 直接返回空列表降级。与 P3-M5-ClassifyError-System-Gap（cache_manager + base_dao 同样未接入）属同源系统性问题。M6 修复仅做 R9 脱敏（`sanitize_error(e)`），未接入错误分类体系。未修复原因：data 层 except 块多为合理降级（返回空列表），接入分类器会改变现有降级语义，需配套测试覆盖每个 except 路径的分类断言，违反 §1.4「微创修改」。相关文件：`data/external/news_fetcher.py`、`utils/error_classifier.py`。 | data 层错误处理统一改造时接入 `classify_error` + `classify_severity`：① `news_fetcher.py` 所有 except 块改用分类器决定日志级别；② 与 P3-M5-ClassifyError-System-Gap 一并修复。验收标准：① data 层全量 except 块接入分类器；② 现有 `test_news_fetcher.py` 全过；③ 新增分类断言单测。upgrade 触发条件：与 P3-M5-ClassifyError-System-Gap 一致（DAO 层错误处理统一改造时）。 |
| **P3-M7-MacroSync-CnGdp-Defensive-EngineDisposed** | **`_sync_macro_monthly` cn_gdp inner try 缺 `except EngineDisposedError: raise` 防御性补全** | M7 模块化检视发现：`data/sync/macro.py:294` 的 `_sync_macro_monthly` cn_gdp inner try 的 `except Exception as e:` 块前未补 `except EngineDisposedError: raise` 守卫。`get_cn_gdp` 是 Tushare API 调用（非 DB 调用），`EngineDisposedError` 实际不太可能从 API 层抛出，触发概率低。M7 修复已完成同文件其他 4 处 R5 守卫（#M7-005/006/007 + 举一反三），此处为防御性补全，按 §1.3「YAGNI」与 §1.4「不做无益重构」推迟。相关文件：`data/sync/macro.py:294`、`data/sync/financial.py:110`（参考实现）。 | `macro.py` 重构或 `EngineDisposedError` 从 API 层抛出时补 `except EngineDisposedError: raise` 守卫。验收标准：① 补全后 `macro.py` 全部 `except Exception as e:` 前均有 `except EngineDisposedError: raise`；② 现有 `test_macro_sync.py` 全过；③ 新增 `EngineDisposedError` 传播单测。upgrade 触发条件：① `macro.py` 重构时；② 或 `EngineDisposedError` 从 API 层抛出（实际场景报告）时。 |
| **P3-M8-HealthMixin-DI-And-Redundant-Imports** | **`health_mixin.py` 4 处函数内 import / `new TushareClient()` 破坏 DI + 冗余 import（既有代码，部分修复）** | M8 模块化检视发现：`data/mixins/health_mixin.py` 的 `check_data_health` 方法存在 4 处 DI / import 反模式（均非本次 R9 修复引入）：① L328 `from utils.time_utils import parse_date` 冗余（顶部 L36 已导入 `parse_date`）；② L331 `from utils.config_handler import ConfigHandler` 函数内 import，违反 DI 原则（应通过 host 类 `DataProcessor` 注入或经 `self.config` 访问）；③ L352-354 `from data.external.tushare_client import TushareClient; tc = TushareClient()` 函数内 new TushareClient，破坏 DI（应通过 `self.api` 或 `self.context.api` 注入，复用 DataProcessor 已初始化的实例与 token）；④ L385 `from utils.time_utils import parse_date as _pd` 冗余（顶部已导入，使用别名 `_pd` 避免遮蔽属反模式）。**部分修复（2026-07-27 TD.6）**：删除 ①④ 两处纯冗余 import（顶部 L36 已导入 `parse_date`，函数内重复 import 是死代码），L387 `_pd(api_latest_official)` 改为 `parse_date(api_latest_official)`；②③（ConfigHandler/TushareClient 函数内 import）保留待 DI 重构，因修复需 host 类暴露 `config`/`api` 属性，跨 `data/mixins/` + `data/data_processor.py` 两层，违反 §1.4「微创修改」。相关文件：`data/mixins/health_mixin.py:328,331,352-354,385`（L328/L385 已删除）、`data/data_processor.py`（host 类）。 | `HealthCheckMixin` 依赖注入重构时：① 移除剩余 2 处函数内 import（L331 `ConfigHandler` / L352-354 `TushareClient`）；② `ConfigHandler` 经 host 类 `self.config` 或构造参数注入；③ `TushareClient` 经 `self.api` 复用 host 已初始化实例。验收标准：① `health_mixin.py` 无函数内 import；② 现有 `test_health_mixin*.py` + `test_data_processor*.py` 全过；③ 新增 DI 注入路径单测。upgrade 触发条件：① `HealthCheckMixin` 重构时；② 或 `DataProcessor` 依赖注入改造时。 |

## 引用关系

- [CLAUDE.md §3.3](../../CLAUDE.md#33--已知技术债与架构限制-known-limitations): 已知技术债与架构限制索引（CLAUDE.md 仅保留摘要 + 指向本文的链接）
- [CONTRIBUTING.md §排查典型问题](../../CONTRIBUTING.md#排查典型问题): 排查典型问题（与技术债互补）
- [docs/guides/ci-cd.md](../guides/ci-cd.md): CI/CD 流水线与门禁（litellm Python 3.14 限制相关 CI 矩阵配置）
