# 已知架构技术债 (Known Technical Debt)

> 来源：从 CONTRIBUTING.md §已知架构技术债 迁移。CONTRIBUTING.md 原章节替换为指向本文的链接。

> Owner: 架构维护者
> 复核触发器: 排查深层问题 / upgrade 触发条件评估 / 红线违规频发 / 重构任务启动时

项目开发演进过程中产生了一些需要明确跟踪的技术债与设计限制，请在排查深层问题时参考。已解决事项（Windows 测试事件循环泄露、V0 兼容垫片删除、声明式迁移收官等）不再列入本表，仅在相关章节的活动规范中标注当前允许/禁止形态。

## 技术债清单

| 级别 | 问题描述 | 产生背景与现状 | 期望的最终解法 |
|------|---------|---------------|--------------|
| **P3** | **strategies/ 层 except Exception 已标记 NOTE(lazy)（ceiling 动态化），待评估统一走 classify_error** | strategies/ 层 except Exception 中 P0/P2 必修项已完成，剩余已合理日志的 except Exception 用 `# NOTE(lazy):` 标记保留。Phase 6.1 杠杆评估决策"不引入 `_handle_strategy_exception(e, context)` 统一入口"（YAGNI：`classify_error` 调用稀少；过度抽象：5 正交维度需 7 字段 context；多处差异化状态修改无法覆盖）。Phase 6.3 完成 ceiling 动态化（按方法分组描述，不再硬编码数值）。R9 合规修复已完成（`str(e)` → `DataSanitizer.sanitize_error(e)`）。 | 策略层重构或新增策略时统一走 `classify_error` + `classify_severity`。upgrade 触发条件：策略层重构时。评审决策：保持现状（NOTE(lazy) 标记已记录 upgrade 条件），强行接入 classify_error 会改变 system severity → raise 行为，违反 §1.4「不做无益重构」。 |
| **P3** | **utils/ 层 except Exception 已标记 NOTE(lazy)，待评估统一走 classify_error** | R3 场景遗漏检视发现：utils/ 层 except Exception（分布在 config_handler / exception_hooks / logger / singleton_registry / diagnostics 等）。已统一标记 `# NOTE(lazy):`（三要素齐全：简化内容 + ceiling + upgrade），`time_utils.py` 中一处候选标记已剔除（无业务消费方语义，调查时判定不适合标记）。0 处适合走 `classify_error`（均无业务消费方，属基础设施兜底或合理降级）。 | 后续 utils 层异常处理统一改造时重新评估是否引入 `classify_error`。upgrade 触发条件：utils 层异常处理统一改造时。 |
| **P3** | **红线自动化覆盖部分实现（R1/R4/R12/R13/R14/R15 已落地，R16 暂缓）** | R1 由 import-linter 4 条契约守护；R4/R12/R13/R14/R15 由 `scripts/check_redlines.py` 实现并接入 pre-commit `redline-check` hook（单元测试守护）。R16（UI 阻塞）暂缓：AST 扫描误报风险高，需更精确的事件处理器识别逻辑。各红线 automation_coverage 与 human_review_required 见 `docs/governance/redlines.yml`。 | R16 暂缓。upgrade 触发条件：R16 误报控制方案成熟或红线违规频发时重新评估。 |
| **P3** | **AIStrategyMixin（2173 行）高度内聚，context builder 拆分推迟评估** | `strategies/ai_mixin.py` 的 `AIStrategyMixin` 当前 2173 行（2026-07-18 复核：旧记录 1890 行已过时），承载 AI 策略 context 构建与决策调用，方法间共享状态密集、内聚度高。Phase 评审判定当前规模未触发可维护性红线（无跨策略复制粘贴、无重复 context 拼接逻辑），强行拆分会引入跨模块状态传递抽象，违反 §1.3「拒绝过度抽象」。文件增长单独不触发 upgrade 条件。 | 未来 AI 策略重构或新增 AI 策略类型时再评估拆分 context builder。验收标准：① context builder 拆为独立模块/类（如 `AIContextBuilder`）；② 拆分后 AI 策略行为不变（现有 AI 策略单测 + 集成测试全过）；③ 新增 context builder 单测覆盖核心分支。upgrade 触发条件：AI 策略重构或新增 AI 策略类型时。 |
| **P3** | **TushareClient（2116 行）capability probe 部分约 400 行可独立拆分** | `data/external/tushare_client.py` 的 `TushareClient` 当前 2116 行（2026-07-18 复核：旧记录 1852 行已过时），其中 capability probe（能力探测）部分约 400 行与主客户端职责正交，可独立拆分。当前未拆分原因：probe 完成后需通知上层刷新配置/缓存，缺少回调通知机制；强行拆分会引入循环依赖。 | 拆分 capability probe 为独立模块（如 `TushareCapabilityProbe`），通过回调或事件通知上层刷新配置/缓存。upgrade 触发条件：TushareClient 行数持续增长影响维护性时。 |
| **P3-WinE2E-Skip** | **Windows E2E 关键路径 skip（已完成：8/8 用例全量解禁，移除 Windows skipif 装饰器）** | 原 8 个 Windows skipif E2E 用例已全量复验并移除 `sys.platform == "win32"` 限制。**Group A (6 onboarding 用例)** 于 2026-07-25 复验解禁；**Group C (`test_settings_log_level_switch`)** 随 PR-478/479 锚点化重构解决死锁根源，本地实测 10.37s 通过后正式移除 skipif；**Group B (`test_wizard_db_validation_*`)** 结合本地离线字体缓存及 `QTRADING_DATABASE_MODE=embedded` 条件分支调优，解禁 Windows 限制。所有用例均已进入 Windows CI 标准回归矩阵。 | 持续监控 Windows CI `e2e-tests-windows` job 运行表现。若 10 个 PR 内出现 flaky 失败（≥2 次），按 Group A 反向回滚机制重新评估。验收标准：① Windows CI `e2e-tests-windows` 8 用例全部运行且通过；② 无 skipif markers。 |
| **P3-Rust-Coverage-D38** | **Rust sidecar unit 覆盖率 run.rs 15.68% 未达 §17.0 门禁 85%（D38，maint.rs/pgbin.rs 已达标）** | pg_plan.md §17.0 要求 Rust unit 行覆盖率 ≥85%。2026-07-28 已补测 `maint.rs`（45.20%→86.29%）与 `pgbin.rs`（45.03%→95.08%）至 85%+，恢复 `sidecar.yml` 的 `--fail-under-lines 75` 门禁（advisory→enforce），移除 NOTE(lazy) 注释。当前 TOTAL 79.87%（`run.rs` 15.68% 仍测试不足，supervisor 难以单测，需 mock postgresql_embedded 或重构可测路径）。门禁底线 75% 防止覆盖率退化；run.rs 达 85% 后提升门禁至 85。相关文件：`sidecars/qtrading-pg-sidecar/src/run.rs`/`maint.rs`/`pgbin.rs`、`.github/workflows/sidecar.yml`、`reviews/pg_plan.md` §22 D38。 | 补测 `run.rs` supervisor 路径至 ≥85% 后：① 提升 `sidecar.yml` 门禁至 `--fail-under-lines 85`；② 更新 pg_plan.md §22 D38 状态为已解决。验收标准：① `cargo llvm-cov --fail-under-lines 85` 通过；② run.rs supervisor 分支有单测覆盖（mock postgresql_embedded 或重构可测路径）。upgrade 触发条件：run.rs supervisor 可测路径重构时。 |
| **P3-E2E-Windows-Embedded-Timeout-Tight** | **E2E Tests (Windows) embedded 模式 step `timeout-minutes` 余量不足（embedded 模式 > 45 分钟 / 原 45 分钟 = 100% 占用）** | PR #291 方案 B 将 E2E Tests (Windows) 从外置 PostgreSQL 改为 embedded 模式（spec.md §3 不变量 1：embedded 为默认）。外置 PostgreSQL 模式（main 分支）总时长 31 分钟（run 30151665758，2026-07-25），原 step `timeout-minutes: 45` 余量 14 分钟（~31%）。embedded 模式需要构建 sidecar binary + PG 16 initdb + 44 个 E2E 测试，总时长 > 45 分钟（run 30183636080 在 45 分钟 timeout 时被取消，未完成）。embedded 模式比外置 PostgreSQL 模式慢的原因：① sidecar 构建（Cargo build）约 3-5 分钟；② PG 16 initdb + sidecar 启动约 5-10 分钟；③ embedded 模式下 DB 操作可能比外置 PostgreSQL 慢。本次 PR #291 修复将 step `timeout-minutes` 从 45 提升至 60（余量 ~15 分钟 / ~25%），属短期止血；长期需优化 E2E Tests 性能或拆分 job。根因在 embedded 模式的资源密集特性（每个 Flet 子进程启动 sidecar + PG 16 initdb），与 Windows runner 性能波动叠加易突破 timeout。相关文件：`.github/workflows/ci_cd.yml` 的 `e2e-tests-windows` job、`tests/e2e/conftest.py`。 | 长期优化方案（任选其一或组合）：① 用 `pytest --durations=20` 识别 embedded 模式下最慢的 20 个 E2E 测试并优化（如减少 sidecar 启动等待时间、并行化独立测试）；② 将 `tests/e2e/` 拆分为 2-3 个并行 job（如按 `test_onboarding_*`、`test_screener_*`、`test_settings_*` 等模块拆分），缩短 wall clock；③ 优化 sidecar 启动流程（如复用 PGDATA 目录、跳过重复 initdb）；④ 评估是否部分 E2E 测试可回退到外置 PostgreSQL 模式（仅 embedded 特性测试用 embedded 模式）。验收标准：① E2E Tests (Windows) embedded 模式实际运行时间 ≤ 40 分钟（余量 ≥ 20 分钟 / ~33%）；② 拆分 job 后各 job wall clock ≤ 30 分钟；③ 现有测试全过且无新增 flaky。upgrade 触发条件：① E2E Tests (Windows) embedded 模式实际运行时间 ≥ 55 分钟（92% 占用 60 分钟 timeout）；② 或连续 3 次 PR 触发 E2E Tests (Windows) 超时；③ 或 E2E 测试总数增长 ≥ 20%（如新增 9 个测试）。 |
| **P3-Fi25-Rust-Injection-Missing** | **§17.6 失败注入场景 #25（数据页 checksum 错误）缺 Rust 集成测试** | pg_plan.md §17.6 失败注入测试矩阵场景 #25：篡改测试表数据页后查询 → PostgreSQL 报 `invalid page in block` → sidecar stdout warning 事件上报 → UI 提示备份恢复（§13.7.38）。`failure_injection.rs` 现覆盖 10 场景（#3/#4/#8/#9/#23/#24/#26/#27/#28/#31），#25 缺失。根因分析（2026-07-23）：① **需协议扩展**——当前 sidecar stdout 协议仅 `ready.v1` / `event.warning.v1` / `event.exit.v1` / `status.v1` / `doctor.v1` / `version.v1` 六类 schema，运行期 PostgreSQL 日志监听 + warning 事件 emission 尚未实现（`run.rs` 的 `supervise` 循环无 postgres stderr/log 关键字扫描）；② **需运行期日志监控**——sidecar 需新增 daemon thread/task 持续读取 PostgreSQL `log_min_messages=warning` 输出，匹配 `invalid page in block` 关键字后经 `protocol::print_json_line(&EventJson::warning(...))` 上报 stdout；③ **需 UI 集成**——Python 侧 `_stdout_reader_task` 需解析 `event.warning` 事件并经 ViewModel state 通知 UI 显示"检测到数据页损坏，建议尽快备份并从最近备份恢复"。三端协同改造 scope 远超 P2 修复边界，母计划策略"对影响正确性的 P2 修复（如 #25/#28）"中 #25 归类为"需协议扩展的设计偏差"，按 §1.3 YAGNI 推迟。**注**：#28（restore 中断残留）已在本次 pg-plan-review 修复（doctor 新增 `scan_residuals` + `restore_residuals` / `dump_partials` 字段 + 集成测试 `test_inject_28_restore_interruption_residual`）。相关文件：`sidecars/qtrading-pg-sidecar/src/run.rs`（supervise 循环）、`src/protocol.rs`（EventJson::warning）、`src/maint.rs`（doctor scan_residuals 已实现）、`data/persistence/embedded_postgres/service.py`（_stdout_reader_task）、`reviews/pg_plan.md` §17.6 #25 / §13.7.38。 | ① sidecar `run.rs` supervise 循环新增 postgres 日志监听 task（持续读 postgres stderr/log file），匹配 `invalid page` / `checksum failure` 关键字后经 `protocol::print_json_line(&EventJson::warning("data_page_corruption", ...))` 上报 stdout；② Python `_stdout_reader_task` 解析 `event.warning` 事件并经 ViewModel state 通知 UI；③ UI 显示数据页损坏警告 + 引导备份恢复；④ `failure_injection.rs` 新增 `test_inject_25_data_page_checksum_error` 集成测试（篡改测试表数据页 → 查询 → 验证 sidecar stdout warning 事件）。验收标准：① sidecar 监听 postgres 日志并上报 `data_page_corruption` warning 事件；② Python + UI 正确显示警告；③ 集成测试覆盖 #25 场景；④ 不破坏现有 stdout 协议向后兼容。upgrade 触发条件：① sidecar 引入运行期 postgres 日志监控需求时；② 或数据页损坏问题在生产环境报告时。 |
| **P3-App-ExcInfo-Traceback-Path-Leak** | **`exc_info=True` 的 traceback 文件路径未经 sanitize_error 脱敏（合理设计决策，供未来评估）** | M3 模块化检视发现：`app/error_logging.py`（`log_exception_with_severity` 函数）与 `app/bootstrap.py` 8 处 `_log(..., exc_info=True)` 调用时，logging 模块从 `sys.exc_info()` 提取 traceback 附加到日志记录，traceback 含文件绝对路径（如 `D:\workspace\qTrading\app\bootstrap.py`）。`DataSanitizer.sanitize_error` 默认 `show_traceback=False`（已确认 `utils/sanitizers.py` 的 `DataSanitizer.sanitize_error` 默认值），只处理 exception 消息字符串，不处理 `exc_info` 附加的 traceback。检视结论：文件路径泄露不是 R9 红线违规（R9 定义为"明文 Token/API Key/密码/个人信息"，文件路径不属于这些类别）；`exc_info=True` 是调试必需的（traceback 对问题定位至关重要），当前实现是"调试可见性"与"信息泄露风险"之间的合理权衡。本次 M3 修复了 `detail=str(e)` → `DataSanitizer.sanitize_error(e)`（FIND-01），但未触及 `exc_info` 的 traceback 路径。相关文件：`app/error_logging.py`（`log_exception_with_severity` 函数）、`app/bootstrap.py`（`_log` 调用处）、`app/startup_controller.py`（`_init_services` 与 `skip` 方法）、`utils/sanitizers.py`（`DataSanitizer.sanitize_error` 的 `show_traceback` 默认值）。 | 如需统一脱敏 traceback 路径：① 自定义 logging Formatter（在日志输出层统一处理 traceback 路径脱敏）；② 或在 `sanitize_error` 中支持 `exc_info` 参数自动提取并脱敏 traceback。验收标准：① traceback 中的文件路径经脱敏后为 `<PATH>`；② 现有 `test_error_logging.py` 全过；③ 新增 traceback 路径脱敏单测。upgrade 触发条件：① 安全审计要求脱敏文件路径时；② 或引入自定义 logging Formatter 时。 |
| **P3-App-StartupPartialFailure-Leak** | **`initialize_services` 启动部分失败时已启动服务资源暂时泄漏（跨模块延后）** | M3 模块化检视发现：`app/bootstrap.py:125-127` 的 `SchedulerService().start()` / `await NewsSubscriptionService().start()` / `await MarketDataService().start()` 三个 start 调用无 try/except 保护。若 `NewsSubscriptionService.start()` 或 `MarketDataService.start()` 抛异常，已启动的 SchedulerService 继续运行。异常传播到 `StartupController._init_services` except 块（startup_controller.py L98），转为 INIT_FAILED 状态。影响：① 用户看到 INIT_FAILED 后若点击 Skip（startup_controller.py L161-165 `skip()`），已启动的服务继续运行直到最终关机；② 最终关机时 `ShutdownCoordinator` Step 1（`utils/shutdown.py:365-387`）会停止所有服务，暂时泄漏可接受；③ `reconfigure()` 仅调用 `cache_manager.close()`，不停止已启动服务。本次 M3 未修复原因：修复需追踪哪些服务已启动并调用各自的 stop 方法（SchedulerService.stop() 同步 / NewsSubscriptionService.stop() async / MarketDataService.stop() async，签名不同），跨 3 个服务模块，需配套测试，违反 §1.3「极简」与 §1.4「微创修改」原则。相关文件：`app/bootstrap.py:125-127`、`app/startup_controller.py:98-114,161-165`、`utils/shutdown.py:365-387`。 | 在 `bootstrap.py:125-127` 外层加 try/except，失败时调用已启动服务的 stop/cleanup。验收标准：① 追踪已启动服务（如用 flag 或 list）；② 异常时按逆序调用各服务 stop 方法；③ 新增 `test_bootstrap.py::TestInitializeServicesStartFailures` 覆盖部分失败场景；④ 现有测试全过。upgrade 触发条件：① 用户报告 Skip 后后台服务干扰；② 或 3 个服务模块重构统一 stop 签名时。 |
| **P3-App-SyncFunctions-In-Async-Bootstrap** | **`initialize_services` 中同步函数（preload_aliases/validate_failover/validate_strategy_tier）阻塞事件循环（合理设计决策）** | M3 模块化检视发现：`app/bootstrap.py:77`（`MetaDataManager.preload_aliases()`）、L131（`_validate_failover_credentials()`）、L134（`_validate_strategy_tier_coverage()`）在 async `initialize_services` 中同步调用，阻塞事件循环。检视结论：① `preload_aliases` 是纯内存操作（遍历 TABLE_DEFINITIONS + I18n.get 翻译），毫秒级；② `_validate_failover_credentials` 首次读配置文件（`config_handler.py:285-291` `open(CONFIG_FILE)`），后续走 `_config_cache`，毫秒级；③ `_validate_strategy_tier_coverage` 调用 `StrategyManager().strategies.keys()` + `validate_strategy_tier_coverage()`，纯内存操作。R16 红线聚焦"Flet 事件处理器中的同步阻塞场景"（如点击按钮后同步等待网络响应），启动期一次性同步操作（毫秒级内存/配置文件操作）不影响用户体验，不适用 R16。修复（用 ThreadPoolManager 包装）会增加复杂度且收益不显著，违反 §1.3「极简」。相关文件：`app/bootstrap.py:77,131,134`、`data/persistence/metadata_manager.py:14-23`、`utils/config_handler.py:279-291`。 | 不主动修复。若未来 `preload_aliases` / `validate_failover_credentials` / `validate_strategy_tier_coverage` 引入网络 IO 或耗时 CPU 操作（如 >100ms），改用 `ThreadPoolManager().run_async(TaskType.IO, ...)` 提交。验收标准：① 改造后启动期 UI 无阻塞；② 现有 `test_bootstrap.py` 全过。upgrade 触发条件：① 这些函数引入网络/磁盘 IO；② 或启动期 UI 响应延迟被用户感知时。 |
| **P3-M4-DbMigrator-OrphanHeal-SingleChain-Assumption** | **`_heal_orphaned_revision` 假设严格线性单链迁移（已文档化设计限制）** | M4 模块化检视发现：`data/persistence/db_migrator.py:149-153` 的 `_heal_orphaned_revision` 注释明确「严格线性单链迁移策略」，「未来引入多分支需重新评估」。当前 15 个迁移（0001~0015）均为单链，假设成立。未来若引入 Alembic 多分支（Multiple Heads），「直接拨回 head」逻辑可能导致 schema 漂移。属于已文档化的设计限制，本次 M4 仅补登记到债表以跨检视周期追溯。相关文件：`data/persistence/db_migrator.py:149-153`、`alembic/versions/0001~0015`。 | 引入 Alembic 多分支时重新评估 `_heal_orphaned_revision` 逻辑：① 检测 Multiple Heads 并拒绝自动 heal；② 或实现多分支 merge 逻辑。验收标准：① 多分支场景下 `_heal_orphaned_revision` 不再「直接拨回 head」；② 新增多分支场景单测。upgrade 触发条件：引入 Alembic 多分支（Multiple Heads）时。 |
| **P3-M4-AppStateService-BroadExcept** | **`app_state_service.py:21,38` broad except 未接入 classify_error（合理降级，一致性建议）** | M4 模块化检视发现：`data/persistence/app_state_service.py:21,38` 共 2 处 `except Exception as e:` 吞没异常返回默认值。app state 是非关键路径（用户偏好/窗口位置等），吞没降级返回默认值是合理设计。但未接入 `classify_error`，与 #M4-002（review_manager 已接入 classify_error）一致性不符。本次 M4 未修复原因：① app state 非关键路径，容错优先；② 修复需引入 `classify_error` + `classify_severity` + system 级 raise 传播，改变现有降级语义，违反 §1.4「不做无益重构」。相关文件：`data/persistence/app_state_service.py:21,38`。 | 与 #M4-002 一致性评估：① 若 app state 升级为关键路径，接入 `classify_error` + `classify_severity`，system 级 raise 传播；② 或维持现状（合理降级）。验收标准：① 若改造，现有 `test_app_state_service.py` 全过；② 新增 system 级异常 raise 传播单测。upgrade 触发条件：① app state 重构为关键路径时；② 或与 #M4-002 一并修复时。 |
| **P3-M6-NewsFetcher-Lazy-ProxyManager-Import** | **`news_fetcher.py:409` 函数内部 import ProxyManager（合理避免循环依赖，建议保留）** | M6 模块化检视发现：`data/external/news_fetcher.py:409` 在 `get_us_major_moves` 内部 `from utils.proxy_manager import ProxyManager`，属函数内部 import。一般而言函数内部 import 是反模式，但此处合理：`utils/proxy_manager.py` 可能间接依赖 `data/external/`（通过配置或单例注册链），顶部 import 会形成循环依赖。M6 未修复原因：修复需重构 `proxy_manager.py` 的依赖方向或提取 proxy 配置为独立模块，跨 `utils/` + `data/external/` 两层，违反 §1.4「微创修改」。相关文件：`data/external/news_fetcher.py:409`、`utils/proxy_manager.py`。 | 若 `proxy_manager.py` 重构解除循环依赖，将 import 移至模块顶部。验收标准：① 顶部 import 无循环依赖；② 现有 `test_news_fetcher.py` 全过。upgrade 触发条件：`proxy_manager.py` 重构或循环依赖被解除时。 |
| **P3-Windows-Unit-Test-Timeout-Tight** | **Windows 单测 step `timeout-minutes` 余量不足（main 成功 run 13 分 41 秒 / 原 15 分钟 = 91% 占用）** | PR #291 触发 Windows 单测 CI 超时调查发现：main 分支最近一次成功 Windows 单测（run #1004，2026-07-25）实际运行 821s（13 分 41 秒），原 step `timeout-minutes: 15` 余量仅 79s（~9%）。main 分支 run #999（2026-07-25 04:48）已实际触发 timeout（运行 909s / 15 分 9 秒，conclusion=failure）。PR #291 触发的 Windows 单测运行 911s（15 分 11 秒）同样触发 timeout。Windows runner 性能波动 + 测试增量（如 PR #291 新增 14 个 mock 单测 + 默认值变更让部分测试走更长路径）易突破 timeout。本次 PR #291 修复将 step `timeout-minutes` 从 15 提升至 20（余量 ~6.5 分钟 / ~32%），属短期止血；长期需优化慢测试或拆分 job。根因不在 PR #291 的代码变更，而在 CI 基础设施层面的余量设计。相关文件：`.github/workflows/ci_cd.yml` 的 `Run Unit Tests` step（Windows job）、`tests/unit/`。 | 长期优化方案（任选其一或组合）：① 用 `pytest --durations=20` 识别 main 分支最慢的 20 个测试并优化（如 mock 重型 IO、减少 fixture 重复初始化）；② 将 `tests/unit/` 拆分为 2-3 个并行 job（如按 `tests/unit/data/`、`tests/unit/ui/`、`tests/unit/strategies/` 等目录拆分），缩短 wall clock；③ 将 `pyproject.toml` 中 `--dist=loadgroup` 切换为 `--dist=loadscope` 减少跨 worker fixture 重复初始化开销。验收标准：① main 分支 Windows 单测实际运行时间 ≤ 12 分钟（余量 ≥ 8 分钟 / ~40%）；② 拆分 job 后各 job wall clock ≤ 8 分钟；③ 现有测试全过且无新增 flaky。upgrade 触发条件：① Windows 单测实际运行时间 ≥ 18 分钟（90% 占用 20 分钟 timeout）；② 或连续 3 次 PR 触发 Windows 单测超时；③ 或单测总数增长 ≥ 20%（如新增 2000 个测试）。 |
| **P3-Data-SyncConcepts-Cancel-Response-2s** | **data 模块 P1-7c：`sync_concepts` 取消响应可能超 2s 红线（原 P1 降级为技术债）** | data 模块 3 轮检视发现：`data/data_processor.py:614` 的 `sync_concepts` 方法中 `fetch_one` 在 `await self.api.get_concept_detail_by_id(c)` 期间无法响应 cancel_event。Tushare `concept_detail` API 有独立严格速率限制（~20 req/min，TokenBucket capacity=5），单次请求可能因 TokenBucket 排队等待 3s+；若同时网络慢，API 调用本身可能阻塞 5-10s。`fetch_one` 在 API 调用前后均检查 `is_cancelled()`，但 API 调用期间无法中断。project_memory 明确要求「Long-running operations must check cancel_event every 2 seconds」。代码注释明确说明设计权衡：「不使用 `wait_for(timeout=N)` — 那会在 event 未 set 时强制等待 N 秒（per-request 限速副作用，与『TokenBucket 唯一限速真相源』冲突）」。**降级原因**：修复需重构 `fetch_one` 的取消传播机制（如引入 cancel-aware TokenBucket、或在 API 调用期间周期性检查 cancel_event），跨 `data/data_processor.py` + `data/external/tushare_client.py` 两层，并发代码改动风险高，违反 §1.3「YAGNI」与 §1.4「微创修改」原则。当前已有部分缓解：① `fetch_one` 在 API 调用前后检查 `is_cancelled()`；② `gather_return_exceptions_propagating_cancel` 传播 `CancelledError`（R2 合规）；③ `concept_detail` API 限速本身限制了单次阻塞上限（~3s TokenBucket 排队 + 网络往返）。相关文件：`data/data_processor.py:614-693`（`sync_concepts` + `fetch_one`）、`data/external/tushare_client.py`（`get_concept_detail_by_id` + TokenBucket 限速）。 | ① 重构 `fetch_one` 的 cancel 传播：在 `await self.api.get_concept_detail_by_id(c)` 期间周期性检查 cancel_event（如用 `asyncio.wait` + `asyncio.sleep(0.5)` 轮询，或引入 cancel-aware TokenBucket）；② 或重构 `TushareClient.get_concept_detail_by_id` 支持取消传播（将 TokenBucket `acquire()` 改为 cancel-aware）。验收标准：① `sync_concepts` 取消响应 ≤2s（即使 API 调用阻塞 10s）；② 现有 `test_data_processor*.py` sync_concepts 测试全过；③ 新增取消响应延迟单测（mock API 阻塞 5s + cancel_event set，断言 2s 内 raise CancelledError）；④ 不破坏 TokenBucket 限速语义。upgrade 触发条件：① 用户报告 sync_concepts 取消响应延迟；② 或 `concept_detail` API 限速调整导致单次阻塞 >5s；③ 或 `TushareClient` TokenBucket 重构时。 |
| **P3-M9-NewsSubscription-EngineDisposed-Swallowed** | **#M9-010 R5: news_subscription_service.py L290-291/394-396/629-631 EngineDisposedError 吞没（设计合理但严格按 R5 应传播）** | M9 services 模块检视发现：`services/news_subscription_service.py:289-291`（`_safe_fetch_task`）/ `:394-396`（`_processing_loop`）/ `:629-631`（`_fetch_and_notify`）共 3 处 `except EngineDisposedError: ... self._running = False / break` — 吞没 EngineDisposedError 以停止轮询。R5 红线："DAO/维护流程必须检查引擎状态；已释放时抛出或传播 EngineDisposedError"。NewsSubscriptionService 不是 DAO/维护流程，是应用服务层的轮询循环，引擎释放后停止轮询是合理设计。但严格按 R5 应传播。M9 未修复原因：设计合理（轮询循环不应因 EngineDisposed 崩溃，而应优雅停止），R5 主要针对 DAO/维护流程；修复需评估是否在 system 级 raise，改变现有降级语义，违反 §1.4「不做无益重构」。相关文件：`services/news_subscription_service.py:289-291/394-396/629-631`。 | 保持现状（合理设计），或在 stop_async 中显式记录 EngineDisposed 触发原因。验收标准：① 若改造，现有 `test_news_subscription_service*.py` 全过；② 新增 EngineDisposedError 触发停止的单测。upgrade 触发条件：① R5 红线扩展到应用服务层轮询循环时；② 或 NewsSubscriptionService 重构时。 |
| **P3-M9-Backtest-EngineDisposed-Warning-Return** | **#M9-011 R5: backtest_service.py L123-140 EngineDisposedError 被捕获返回带警告结果（设计模式不一致）** | M9 services 模块检视发现：`services/backtest_service.py:111-140`（`_persist_result`）的 `except Exception as e` 捕获 EngineDisposedError → `classify_severity(e)` 返回 `"system"`（M4 修复后 error_classifier 已将 EngineDisposedError 加入 SYSTEM_LEVEL_ERROR_TYPES）→ `logger.critical` 但不 raise，返回 `result.with_warnings(new_warnings)`。这意味着数据库 disposed 后回测结果仍带警告返回，但后续操作也会失败。`_persist_result` 的语义是"持久化失败不阻塞回测结果返回"，从业务角度合理（回测已完成，持久化失败只是警告）。但 EngineDisposedError 意味着数据库不可用，与 task_manager 的 system 级 critical log 不 raise 模式一致。M9 未修复原因：设计模式不一致但业务合理，修复需评估是否在 system 级 raise，改变现有降级语义，违反 §1.4「不做无益重构」。相关文件：`services/backtest_service.py:111-140`。 | 保持现状（合理设计），或评估是否在 system 级 raise。验收标准：① 若改造，现有 `test_backtest_service*.py` 全过；② 新增 EngineDisposedError 触发 system 级 raise 的单测。upgrade 触发条件：① R5 红线扩展到回测持久化路径时；② 或 BacktestService 重构时。 |
| **P3-M9-NewsSubscription-ClassifyError-Gap** | **#M9-012 模式不一致：news_subscription_service.py 仅用 classify_severity 不用 classify_error** | M9 services 模块检视发现：`services/news_subscription_service.py:13` 仅导入 `classify_severity`，未导入 `classify_error`；与其他 services（ai_service/task_manager/local_model_manager/backtest_service 共 88 处同时用两者）模式不一致。NewsService 功能正常（`classify_severity` 已满足分级日志需求），但无法获取结构化 error_info（code/message_key）。M9 未修复原因：接入 `classify_error` 需重构 7 处 except 块，违反 §1.4「不做无益重构」。相关文件：`services/news_subscription_service.py`。 | 若 NewsSubscriptionService 重构，统一接入 `classify_error` + `classify_severity`。验收标准：① 7 处 except 块使用 `error_info = classify_error(e, context=...)` + `severity = classify_severity(e)`；② 现有 `test_news_subscription_service*.py` 全过。upgrade 触发条件：① NewsSubscriptionService 重构时；② 或 services 层错误处理模式统一审计时。 |
| **P3-M9-EmbeddedPg-TimeoutExpired-Propagation** | **#M9-013 错误处理缺口：embedded_pg_maintenance_service.py subprocess TimeoutExpired 未分类处理** | M9 services 模块检视发现：`services/embedded_pg_maintenance_service.py` `_run_sidecar` → `_run` 函数（约 L297）的 `subprocess.run(argv, capture_output=True, text=True, timeout=3600)` 的 `TimeoutExpired` 异常未在公开方法（doctor/dump/restore/maintenance_shell）中分类处理，会直接传播到调用方。与项目错误分类模式不一致（其他 services 用 `classify_error` + `classify_severity`）。M9 未修复原因：`TimeoutExpired` 传播是合理设计（调用方应处理）；在公开方法中捕获会改变现有异常契约，影响调用方。相关文件：`services/embedded_pg_maintenance_service.py`。 | 若 sidecar CLI 超时频繁触发，在公开方法中捕获 `TimeoutExpired` 并分类处理。验收标准：① `TimeoutExpired` 经 `classify_error` 分类；② 现有 `test_embedded_pg_maintenance*.py` 全过。upgrade 触发条件：① sidecar CLI 超时频繁触发时；② 或 EmbeddedPgMaintenanceService 重构时。 |
| **P3-M9-TaskManager-LogAsyncOperation-Threshold** | **#M9-014 性能监控阈值：task_manager.py cancel_all_running_async 标注 DB_SINGLE_QUERY 但实际是批量操作** | M9 services 模块检视发现：`services/task_manager.py:542` `cancel_all_running_async` 方法标注 `@log_async_operation(threshold_ms=PerfThreshold.DB_SINGLE_QUERY)`，但该方法实际是批量取消+等待，可能更适合 `DB_BULK_IO`。阈值选择影响性能监控告警精度，不影响功能。M9 未修复原因：修改需评估历史告警基线。相关文件：`services/task_manager_manager.py:542`。 | 若性能监控告警基线评估后确认阈值不当，改为 `PerfThreshold.DB_BULK_IO`。验收标准：① 告警基线评估完成；② 现有 `test_task_manager*.py` 全过。upgrade 触发条件：① 性能监控告警误报/漏报时；② 或 TaskManager 性能监控重构时。 |
| **P3-M10-StrategyManager-Registry-Reset-Limitation** | **#M10-003 残余风险：`_reset_singleton` 不清空 `_STRATEGY_REGISTRY`，mock 策略与真实策略 key 冲突时 mock 会覆盖真实策略** | M10 strategies 模块检视发现：`strategies/all_strategies.py` 的 `StrategyManager._reset_singleton` 仅重置 `_instance` / `_initialized` / 模块级 `_strategies_imported` 标志，不清空 `_STRATEGY_REGISTRY`。根因：Python import 是幂等的，清空 registry 后重新执行 `_import_all_strategies()` 不会重新触发 `@register_strategy` 装饰器（装饰器仅在首次 import 时执行），会导致真实策略丢失。M10-003 修复方案选择"仅重置 `_strategies_imported`"而非"清空 registry + 重置 `_strategies_imported`"，残余风险：测试中若 mock 策略使用与真实策略相同的 key（如 `"volume_breakout"`），mock 会覆盖真实策略且无法通过重新 import 恢复。现有测试模式不触发该场景（测试中使用独特 key 的 mock 策略或 `patch("...get_strategy_registry")` 隔离）。相关文件：`strategies/all_strategies.py`（`_reset_singleton` docstring）、`strategies/base_strategy.py`（`@register_strategy` 装饰器）。 | 若 Python import 机制变更或 `@register_strategy` 改为可重新触发（如使用 `importlib.reload`），可恢复 registry 清空逻辑。验收标准：① `_reset_singleton` 清空 registry 后下次实例化能完整加载真实策略；② 新增 mock 覆盖真实策略的回归测试；③ 现有 `test_strategy_manager_reset.py` 全过。upgrade 触发条件：① 测试模式变更导致 mock 策略与真实策略 key 冲突时；② 或 `@register_strategy` 装饰器支持重新触发时；③ 或策略注册机制重构时。 |
| **P2-M11-StartupViews-App-Layer-Import** | **#M11-001 架构边界：ui/startup_views.py 导入 app 层（已登记例外 EX-0001）** | M11 ui 基础设施模块检视发现：`ui/startup_views.py:28-29` 导入 `app.bootstrap.EmbeddedPgStartupScenario` 和 `app.startup_controller.{StartupContext, StartupController, StartupState}`，违反 §4.1「app → 编排所有层，仅被 main.py 调用」与 `tests/unit/test_architecture_boundaries.py` 中 `FORBIDDEN_IMPORTS["ui"] = ["app"]` 约束。**已通过例外注册表显式登记**（[docs/governance/exceptions.yml](../governance/exceptions.yml) EX-0001，rule_id=R1），注释说明"main.py 启动装配链延伸，非 ui 层正常业务导入"，`test_known_exceptions_are_valid` 守护例外文件持续存在。M11 未修复原因：修复需将 StartupView 移至 app/ 层或 main.py 内联装配，属架构重构，违反 §1.4「不做无益重构」。相关文件：`ui/startup_views.py:25-29`、`docs/governance/exceptions.yml`。 | 长期考虑将 StartupView 移至 app/ 层或在 main.py 内联装配，彻底消除该例外。验收标准：① `FORBIDDEN_IMPORTS["ui"]` 中移除 `"app"` 或例外注册表清除 `ui/startup_views.py`（EX-0001）；② 现有 `test_architecture_boundaries.py` + `test_startup_views*.py` 全过。upgrade 触发条件：① 启动流程重构时；② 或 app 层与 ui 层边界重新划分时。 |
| **P3-M11-StartupViews-HomeViewModel-Static-Call** | **#M11-002 MVVM 边界：startup_views.py View 直接调用 HomeViewModel 静态方法** | M11 ui 基础设施模块检视发现：`ui/startup_views.py:595-606` `_setup_news_alert` 中 `HomeViewModel.register_news_alert_listener(on_news_alert)`，`ui/startup_views.py:608-615` `_cleanup_news_alert` 中 `HomeViewModel.unregister_news_alert_listener(cb)`。View 直接 import 并调用 `HomeViewModel` 静态方法注册/注销 news alert listener。严格 MVVM 边界下，View 应通过自身消费的 ViewModel 实例命令转发。已有注释说明（startup_views.py:588-590）"CLAUDE.md §3.2 MVVM: View 不直调 NewsSubscriptionService, 经 HomeViewModel 命令转发（合规范例 home_view_model.py:111）"。cleanup 完整（unregister + ref 置 None），无内存泄漏。M11 未修复原因：修复需引入 StartupViewModel 抽象，属过度设计，违反 §1.3「极简设计」。相关文件：`ui/startup_views.py:587-617`、`ui/viewmodels/home_view_model.py:111`。 | 长期重构为通过 StartupView 自身消费的 VM 命令转发（需引入 StartupViewModel 或类似抽象）。验收标准：① View 不直接调 HomeViewModel 静态方法；② 现有 `test_startup_views*.py` 全过。upgrade 触发条件：① 启动流程重构时；② 或 news alert 监听机制重构时。 |
| **P3-M12-BacktestResultPanel-TypeChecking-Import** | **#M12-007 R1 边界：backtest_result_panel.py TYPE_CHECKING 跨层导入** | M12 ui 表现层模块检视发现：`ui/components/backtest/backtest_result_panel.py:34-35` `if TYPE_CHECKING: from strategies.backtest.config import BacktestResult`。严格按 R1 字面规定属违规（ui 导入 strategies），但业界惯例通常允许 TYPE_CHECKING 块内的跨层类型导入（运行时不导入，仅用于类型提示）。M12 未修复原因：修复需引入协议类型（Protocol）或 ViewModel 重新封装数据结构，跨多文件协调，违反 §1.4「微创修改」。相关文件：`ui/components/backtest/backtest_result_panel.py:34-35`。 | 项目宪法明确豁免或禁止 TYPE_CHECKING 跨层导入。验收标准：① 若禁止，引入 Protocol 或 VM 封装；② 现有 `test_backtest_result_panel*.py` 全过。upgrade 触发条件：① 项目宪法明确 TYPE_CHECKING 政策时；② 或 backtest_result_panel 重构时。 |
| **P3-M12-OnboardingViewModel-StepValidated-Mutable-Property** | **#M12-010 MVVM 契约：screener_view_model.py step_validated property 暴露可变 dict** | M12 ui 表现层模块检视发现：`ui/viewmodels/onboarding_view_model.py:229-231` `@property def step_validated(self) -> dict[str, bool]: return self._step_validated`。OnboardingState 是 frozen dataclass（合规），但 VM 通过公开 property 直接暴露内部可变 dict，绕过 state snapshot 契约。M12 未修复原因：修复需引入 OnboardingState 字段重设计 + onboarding_wizard.py 渲染期 vm.bind 副作用迁移到 use_effect（行 358-369），跨多文件协调。相关文件：`ui/viewmodels/onboarding_view_model.py:229-231`、`ui/views/onboarding_wizard.py:358-369`。 | OnboardingState 字段重设计，将 step_validated 纳入 frozen state。验收标准：① step_validated 纳入 state snapshot；② onboarding_wizard.py 副作用迁移到 use_effect；③ 现有 `test_onboarding*.py` 全过。upgrade 触发条件：① OnboardingViewModel 重构时；② 或 onboarding_wizard 重构时。 |
| **P3-M12-StockDetailDialog-View-Holds-Page-DataProcessor** | **#M12-014 MVVM 边界：stock_detail_dialog.py View 直接持有 page/data_processor 引用** | M12 ui 表现层模块检视发现：`ui/components/stock_detail_dialog.py` View 直接持有 `page` 和 `data_processor` 引用（过渡期设计）。严格 MVVM 边界下，View 不应直接持有 page/data_processor，应通过 ViewModel 消费。M12 未修复原因：修复需引入 StockDetailViewModel，跨多文件协调，属 MVVM 完全改造。相关文件：`ui/components/stock_detail_dialog.py`。 | 引入 StockDetailViewModel，View 通过 VM 消费 page/data_processor。验收标准：① View 不直接持有 page/data_processor；② 现有 `test_stock_detail_dialog*.py` 全过。upgrade 触发条件：① stock_detail_dialog MVVM 改造时；② 或 ui 层 MVVM 完全迁移时。 |
| **P3-M12-BacktestConfigPanel-No-VM** | **#M12-015 MVVM 边界：backtest_config_panel.py 11 个 use_state 未建 VM** | M12 ui 表现层模块检视发现：`ui/components/backtest/backtest_config_panel.py` 使用 11 个 `use_state` 管理状态，未建立 ViewModel。严格 MVVM 边界下，状态应由 VM 管理。M12 未修复原因：修复需引入 BacktestConfigViewModel + 迁移 11 个 use_state，跨多文件协调。相关文件：`ui/components/backtest/backtest_config_panel.py`。 | 引入 BacktestConfigViewModel，迁移 11 个 use_state 到 VM state。验收标准：① 11 个 use_state 迁移到 VM；② 现有 `test_backtest_config_panel*.py` 全过。upgrade 触发条件：① backtest_config_panel MVVM 改造时；② 或 ui 层 MVVM 完全迁移时。 |
| **P3-M12-BacktestResultPanel-Consumes-DataFrame-Directly** | **#M12-016 L771 违规：backtest_result_panel.py View 直接消费 Polars DataFrame** | M12 ui 表现层模块检视发现：`ui/components/backtest/backtest_result_panel.py` View 直接消费 Polars DataFrame（L771 违规）。严格 MVVM 边界下，View 不应直接消费 data 层 DataFrame，应通过 VM 转换为 UI 友好数据结构。M12 未修复原因：修复需引入 BacktestResultViewModel + DataFrame → UI 数据结构转换，跨多文件协调。相关文件：`ui/components/backtest/backtest_result_panel.py`。 | 引入 BacktestResultViewModel，DataFrame → UI 数据结构转换在 VM 层完成。验收标准：① View 不直接消费 DataFrame；② 现有 `test_backtest_result_panel*.py` 全过。upgrade 触发条件：① backtest_result_panel MVVM 改造时；② 或 ui 层 MVVM 完全迁移时。 |
| **P3-M12-ScreenerViewModel-State-Mutable-Types** | **#M12-017 MVVM 契约：screener_view_model.py state 可变类型** | M12 ui 表现层模块检视发现：`ui/viewmodels/screener_view_model.py` state 含可变类型（`strategies_with_dep` / `HistoryTreeRow.strategies` / `get_current_page_data` DataFrame dual-track）。严格 MVVM 契约下，state 应为不可变 frozen dataclass。M12 未修复原因：修复需重设计 ScreenerState 字段类型 + 调用方适配，跨多文件协调。相关文件：`ui/viewmodels/screener_view_model.py`。 | ScreenerState 字段重设计为不可变类型。验收标准：① state 字段全部不可变；② 现有 `test_screener_view_model*.py` 全过。upgrade 触发条件：① ScreenerViewModel 重构时；② 或 ui 层 MVVM 完全迁移时。 |
| **P3-M12-HealthScanViewModel-State-Result-Dict** | **#M12-018 MVVM 契约：health_scan_view_model.py HealthScanState.result: dict[str, Any]** | M12 ui 表现层模块检视发现：`ui/viewmodels/health_scan_view_model.py` `HealthScanState.result: dict[str, Any]` 是可变类型。严格 MVVM 契约下，state 应为不可变 frozen dataclass。M12 未修复原因：修复需重设计 HealthScanState.result 为 frozen dataclass + 调用方适配，跨多文件协调。相关文件：`ui/viewmodels/health_scan_view_model.py`。 | HealthScanState.result 重设计为 frozen dataclass。验收标准：① result 字段不可变；② 现有 `test_health_scan_view_model*.py` 全过。upgrade 触发条件：① HealthScanViewModel 重构时；② 或 ui 层 MVVM 完全迁移时。 |
| **P3-M12-Views-Import-Complexity-High** | **#M12-019 可维护性：screener_view.py (26 import) / data_view.py (22 import) 导入复杂度过高** | M12 ui 表现层模块检视发现：`ui/views/screener_view.py` 26 个 import，`ui/views/data_view.py` 22 个 import，导入复杂度过高。高导入复杂度通常表明文件职责过重。M12 未修复原因：修复需拆分文件职责，属大型重构。相关文件：`ui/views/screener_view.py`、`ui/views/data_view.py`。 | 拆分 screener_view.py / data_view.py 职责到子组件或工具模块。验收标准：① 单文件 import 数 ≤ 15；② 现有测试全过。upgrade 触发条件：① screener_view / data_view 重构时；② 或 ui 层模块拆分时。 |
| **P3-M12-VirtualTable-ArrowSort-Indicator** | **#M12-020 可访问性：virtual_table.py:147 ↑↓ 排序指示器** | M12 ui 表现层模块检视发现：`ui/components/virtual_table.py:147` 使用 ↑↓ 文本符号作为排序指示器。属性技术符号非 emoji，但依赖字体支持。M12 未修复原因：修复需替换为 `ft.Icon`，但需评估布局影响。相关文件：`ui/components/virtual_table.py:147`。 | 替换为 `ft.Icon(ft.Icons.ARROW_UPWARD)` / `ft.Icon(ft.Icons.ARROW_DOWNWARD)`。验收标准：① 排序指示器使用 ft.Icon；② 现有 `test_virtual_table*.py` 全过。upgrade 触发条件：① virtual_table 重构时；② 或可访问性 audit 时。 |
| **P3-M12-TushareConfigPanel-Referrer-Code-In-Url** | **#M12-021 隐私：tushare_config_panel.py:34 URL 含 referrer code** | M12 ui 表现层模块检视发现：`ui/viewmodels/tushare_config_panel_view_model.py` `_TUSHARE_REGISTER_URL = "https://tushare.pro/register?reg=728426"` 含 referrer code（reg=728426）。用户点击注册链接会带 referrer code，可能涉及推广分成。M12 未修复原因：移除 referrer code 需评估是否影响项目维护者收益，属业务决策。相关文件：`ui/viewmodels/tushare_config_panel_view_model.py`。 | 评估是否移除 referrer code。验收标准：① 若移除，URL 改为 `https://tushare.pro/register`；② 现有测试全过。upgrade 触发条件：① 项目维护者决策时；② 或隐私 audit 时。 |
| **P3-M12-ViewModels-Repeated-Singleton-Calls** | **#M12-022 性能：VM 中重复调用 TushareClient()/TaskManager() 等单例** | M12 ui 表现层模块检视发现：VM 中重复调用 `TushareClient()` / `TaskManager()` 等单例（每次调用走 `@register_singleton` 的 `__new__` 路径）。虽然单例模式保证只创建一个实例，但重复调用有轻微性能开销。M12 未修复原因：修复需在 VM `__init__` 中缓存单例引用，但需评估生命周期（VM dispose 时是否需要释放）。相关文件：`ui/viewmodels/` 下多个文件。 | 在 VM `__init__` 中缓存单例引用。验收标准：① VM 不重复调用单例；② 现有测试全过。upgrade 触发条件：① VM 性能优化时；② 或单例模式重构时。 |
| **P3-M12-FailoverConfigPanel-BtnSave-No-RunTask** | **#M12-023 一致性：failover_config_panel.py btn_save 未通过 _run_task_no_args 提交** | M12 ui 表现层模块检视发现：`ui/components/config_panels/failover_config_panel.py` `btn_save` 未通过 `_run_task_no_args` 提交，与同模块其他按钮模式不一致。M12 未修复原因：修复需评估 btn_save 是否需要异步提交，属行为变更。相关文件：`ui/components/config_panels/failover_config_panel.py`。 | 评估 btn_save 是否需要 `_run_task_no_args` 提交。验收标准：① btn_save 与同模块按钮模式一致；② 现有 `test_failover_config_panel*.py` 全过。upgrade 触发条件：① failover_config_panel 重构时；② 或 ui 层异步模式统一时。 |
| **P3-M12-Views-Get-DataProcessor-Cross-Layer** | **#M12-025 MVVM 边界：data_view.py / data_source_tab.py View 获取 data_processor 传子组件** | M12 ui 表现层模块检视发现：`ui/views/data_view.py:1014` / `ui/views/settings_tabs/data_source_tab.py:1014` View 获取 `data_processor` 并传给子组件。严格 MVVM 边界下，View 不应直接获取 data_processor，应通过 VM 消费。M12 未修复原因：修复需引入子组件 VM，跨多文件协调。相关文件：`ui/views/data_view.py:1014`、`ui/views/settings_tabs/data_source_tab.py:1014`。 | 引入子组件 VM，View 不直接获取 data_processor。验收标准：① View 不直接获取 data_processor；② 现有测试全过。upgrade 触发条件：① data_view / data_source_tab MVVM 改造时；② 或 ui 层 MVVM 完全迁移时。 |
| **P3-M12-ChartUtils-ModuleLevel-Mplfinance-Import** | **#M12-026 性能：chart_utils.py:15 模块级 import mplfinance 导致无谓依赖加载** | M12 ui 表现层模块检视发现：`ui/components/chart_utils.py:15` 模块级 `import mplfinance` 导致无谓依赖加载。mplfinance 是重型库，模块级 import 会增加应用启动时间。M12 未修复原因：修复需改为函数内 import，但需评估是否影响类型提示。相关文件：`ui/components/chart_utils.py:15`。 | 改为函数内 import（延迟加载）。验收标准：① mplfinance 延迟到函数内 import；② 现有 `test_chart_utils*.py` 全过。upgrade 触发条件：① chart_utils.py 重构时；② 或应用启动性能优化时。 |
| **P3-M12-HomeView-Render-Phase-Logger-Side-Effect** | **#M12-027 声明式 UI：home_view.py:70/73/75/255 渲染期 logger.info 4 处** | M12 ui 表现层模块检视发现：`ui/views/home_view.py:70/73/75/255` 在 `@ft.component` 渲染期调用 `logger.info` 4 处。声明式 UI 范式下，渲染期应为纯函数，不应有副作用（logger.info 是副作用）。M12 未修复原因：修复需将 logger.info 迁移到 use_effect 或 VM，跨多文件协调。相关文件：`ui/views/home_view.py:70/73/75/255`。 | 将 logger.info 迁移到 use_effect 或 VM。验收标准：① 渲染期无 logger.info；② 现有 `test_home_view*.py` 全过。upgrade 触发条件：① home_view 重构时；② 或声明式 UI audit 时。 |
| **P3-M12-NewsFeed-Callback-Passes-ControlEvent** | **#M12-028 MVVM 边界：news_feed.py:128 on_load_more_click 传递 ControlEvent 而非业务数据** | M12 ui 表现层模块检视发现：`ui/components/news_feed.py:128` `on_load_more_click` 传递 `ControlEvent` 而非业务数据。严格 MVVM 边界下，回调应传递业务数据而非 Flet 控件事件。M12 未修复原因：修复需重构回调签名，跨多文件协调。相关文件：`ui/components/news_feed.py:128`。 | 重构回调签名为传递业务数据。验收标准：① 回调不传递 ControlEvent；② 现有 `test_news_feed*.py` 全过。upgrade 触发条件：① news_feed 重构时；② 或 ui 层 MVVM 完全迁移时。 |
| **P3-M12-ToastManager-Not-Registered-Singleton** | **#M12-030 单例：toast_manager.py ToastManager 未注册 @register_singleton** | M12 ui 表现层模块检视发现：`ui/components/toast_manager.py` `ToastManager` 未注册 `@register_singleton`。CLAUDE.md §4.3 要求所有单例必须使用 `@register_singleton` 注册。ToastManager 是事实上的单例（通过 `ft.context.page` 访问），但未注册。M12 未修复原因：修复需评估 ToastManager 是否真为单例 + 注册 + 实现 `_reset_singleton`，跨多文件协调。相关文件：`ui/components/toast_manager.py`。 | 评估 ToastManager 是否需要注册为单例。验收标准：① 若需要，注册 `@register_singleton` + 实现 `_reset_singleton`；② 现有 `test_toast_manager*.py` 全过。upgrade 触发条件：① ToastManager 重构时；② 或单例 audit 时。 |
| **P3-M12-StockDetailDialog-BuildContent-No-Cover** | **#M12-031 测试覆盖：stock_detail_dialog.py:334 _build_content pragma: no cover** | M12 ui 表现层模块检视发现：`ui/components/stock_detail_dialog.py:334` `_build_content` 标记 `# pragma: no cover`。该函数是 UI 渲染逻辑，标记 no cover 意味着无测试覆盖。M12 未修复原因：修复需补充测试，但 UI 渲染测试较复杂。相关文件：`ui/components/stock_detail_dialog.py:334`。 | 补充 `_build_content` 测试覆盖。验收标准：① 移除 `# pragma: no cover`；② 现有 `test_stock_detail_dialog*.py` 全过。upgrade 触发条件：① stock_detail_dialog 重构时；② 或测试覆盖 audit 时。 |
| **P3-PR373-Viewport-Collapse-Audit** | **增量变更触发边缘状态视口塌陷同类风险排查（PR #373 举一反三）** | PR #373 定位根因为"main 基线视口 6px 边缘状态 + 新增 backtest_btn 增量压缩 → table_card 视口 0px → Column 节点 text=''"（见 [docs/flet/project-differences.md §4.9](../flet/project-differences.md#49-增量变更触发边缘状态视口塌陷pr-373)）。按 CLAUDE.md §1.7「举一反三」需排查同类隐患：`Row(wrap=True)` + `expand=True` + 同级控件增长模式分布在 3 个文件（`ui/views/screener_view.py` / `ui/views/settings_tabs/data_source_tab.py` / `ui/components/config_panels/failover_config_panel.py`），跨 views + components 两层，按 §1.7「>3 文件或跨多层须记录为独立重构任务延后处理」登记为独立任务。当前未排查这些视图是否存在 main 基线视口边缘状态 + 增量变更触发塌陷的同类风险。相关文件：`ui/views/screener_view.py`、`ui/views/settings_tabs/data_source_tab.py`、`ui/components/config_panels/failover_config_panel.py`。 | 排查 3 个文件的 `Row(wrap=True)` + `expand=True` 组合是否存在边缘状态视口风险：① 用 Playwright DOM dump 测量各视图 main 基线表体视口高度；② 若存在边缘状态（接近 0px），评估新增控件触发塌陷的概率；③ 必要时调整布局（如 `expand=2` 或独立右对齐行）。验收标准：① 3 个视图 main 基线视口高度已测量并记录；② 边缘状态视图已修复或标记 NOTE(lazy)；③ 现有 E2E 测试全过。upgrade 触发条件：① 上述视图新增控件触发 E2E 文本超时失败时；② 或布局重构时。 |
| **P3-PR3-TextButton-Interactive-Unverified** | **PR-3 对抗性检视 M3：ft.TextButton + AnchorKind.INTERACTIVE 的 CanvasKit 行为未验证** | `ui/views/onboarding_wizard.py` 的 SKIP_BUTTON 用 `ft.TextButton` + `AnchorKind.INTERACTIVE`。PoC 只验证了 `ft.Button` 的 INTERACTIVE 通道（生成 `flt-tappable`），未验证 `ft.TextButton`。`_locate_inner_tappable_bbox` 有回退逻辑（`flt-tappable` 不存在时回退到外层 `aria-label` 节点），但回退 bbox 可能偏离。当前 `click_skip` 未被任何测试使用。相关文件：`ui/views/onboarding_wizard.py`、`tests/e2e/helpers/anchor_page.py`、`ui/testing/e2e_ids.py`。 | ① 在 E2E smoke test 中验证 `ft.TextButton` 的 CanvasKit DOM 生成行为；② 若不生成 `flt-tappable`，将 `SKIP_BUTTON` 改为 `AnchorKind.COMPLEX` 或将 `ft.TextButton` 改为 `ft.Button`。验收标准：① `click_skip` E2E 测试通过；② bbox 点击坐标命中按钮。upgrade 触发条件：① 新增使用 `click_skip` 的测试时；② 或 `ft.TextButton` 升级时。 |
| **P3-PR3-MutPool-Locale-Restore-No-Fallback** | **PR-3 对抗性检视 M6：test_settings_language_switch finally 块还原失败时 mut pool 内连坐污染** | PR-3 删除 `_ensure_locale_zh` 后，`flet_app_mut` pool 的 locale 还原仅依赖 `test_settings_language_switch` finally 块。若 UI 操作失败（`sp.select_language(lang_zh)` 超时），`flet_app_mut` 内存 locale 停留 en_US，影响后续 `mutates_config` 测试。**设计权衡**：Plans.md Task 3.4 明确要求删除 `_ensure_locale_zh`，read-only 池不受影响。测试进程 I18n 已由 `I18n.set_locale("zh")` 在 UI 操作前还原。相关文件：`tests/e2e/test_settings_flow.py:67-88`、`tests/e2e/conftest.py`。 | 在 `pristine_config` teardown 时为 mut pool 增加 locale 还原兜底（类似原 `_ensure_locale_zh` 但限定 mut pool）。验收标准：① finally 块失败后 mut pool 后续测试仍能正常运行；② read-only pool 不受影响。upgrade 触发条件：① CI 出现 mutates_config 测试连坐失败时；② 或 mut pool 测试数量增长 ≥50% 时。 |
| **P3-PR513-Script-SysPath-Inject** | **PR #513 检视：`scripts/verify_embedded_pg_connect.py` 顶部 `sys.path.insert` 注入仓库根后 import data 层（代码味道，已文档化）** | PR #513 检视（第一轮）遗留观察级建议。`scripts/verify_embedded_pg_connect.py:26-32` 因脚本经 `python scripts/verify_embedded_pg_connect.py` 直接运行时 `sys.path[0]` 指向 `scripts/`，data 包不在可导入路径上，故用 `sys.path.insert(0, str(_REPO_ROOT))` 将仓库根注入 `sys.path` 后 `import data.persistence.embedded_postgres.version`。注入后 `data/__init__.py`、`data/persistence/__init__.py` 均用惰性 `__getattr__`，不触发重依赖，且非红线 `data/cache` 路径，可接受。`sys.path` 注入为代码味道。相关文件：`scripts/verify_embedded_pg_connect.py:26-32`。 | 声明脚本为可安装入口（如 `pyproject.toml` 的 `[project.scripts]`）或以包内相对导入方式运行，消除 `sys.path` 注入。验收标准：① 移除 `sys.path.insert` 后 `python -m scripts.verify_embedded_pg_connect` 可运行；② 现有使用方式（`python scripts/verify_embedded_pg_connect.py`）不回退。upgrade 触发条件：① `scripts/` 其他脚本出现同类 `sys.path` 注入需统一时；② 或脚本迁移为正式包入口时。 |
| **P3-PR513-VersionCache-No-Prod-Invalidation** | **PR #513 检视：`_version_cache` 生产期不主动失效（合理设计，供未来评估）** | PR #513 检视（第一轮）遗留观察级建议。`data/persistence/embedded_postgres/version.py` 的模块级 `_version_cache` 由 `_version_lock` 保护临界区，缓存仅缓存成功结果，同一进程生命周期内假定 sidecar 二进制不随版本变更。`clear_pg_version_cache()` 已提供手动失效入口，但生产路径不主动调用：应用运行期间若 sidecar 被重新安装为不同 PG 主版本，缓存不会自动失效，需外部显式调用 `clear_pg_version_cache()`。当前应用运行期不替换 sidecar，属合理设计。相关文件：`data/persistence/embedded_postgres/version.py`。 | 若应用运行期引入 sidecar 热替换/升级流程，在替换后自动调用 `clear_pg_version_cache()`。验收标准：① sidecar 替换后下次 `resolve_pg_major_version` 重新解析；② 现有 `test_version.py` 全过；③ 新增 sidecar 替换后缓存失效单测。upgrade 触发条件：① 应用引入运行期 sidecar 升级/替换流程时；② 或 EmbeddedPostgresService 支持热切换时。 |

## 引用关系

- [CLAUDE.md §3.3](../../CLAUDE.md#33--已知技术债与架构限制-known-limitations): 已知技术债与架构限制索引（CLAUDE.md 仅保留摘要 + 指向本文的链接）
- [CONTRIBUTING.md §排查典型问题](../../CONTRIBUTING.md#排查典型问题): 排查典型问题（与技术债互补）
- [docs/guides/ci-cd.md](../guides/ci-cd.md): CI/CD 流水线与门禁（litellm Python 3.14 限制相关 CI 矩阵配置）
