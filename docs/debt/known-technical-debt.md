# 已知架构技术债 (Known Technical Debt)

> 来源：从 CONTRIBUTING.md §已知架构技术债 迁移。CONTRIBUTING.md 原章节替换为指向本文的链接。

> Owner: 架构维护者
> 复核触发器: 排查深层问题 / upgrade 触发条件评估 / 红线违规频发 / 重构任务启动时

项目开发演进过程中产生了一些需要明确跟踪的技术债与设计限制，请在排查深层问题时参考。已解决事项（Windows 测试事件循环泄露、V0 兼容垫片删除、声明式迁移收官等）不再列入本表，仅在相关章节的活动规范中标注当前允许/禁止形态。

## 技术债清单

| 级别 | 问题描述 | 产生背景与现状 | 期望的最终解法 |
|------|---------|---------------|--------------|
| **P3** | **strategies/ 层 except Exception 已标记 NOTE(lazy)（ceiling 动态化），待评估统一走 classify_error** | strategies/ 层 except Exception 中 P0/P2 必修项已完成，剩余已合理日志的 except Exception 用 `# NOTE(lazy):` 标记保留。Phase 6.1 杠杆评估决策"不引入 `_handle_strategy_exception(e, context)` 统一入口"（YAGNI：`classify_error` 调用稀少；过度抽象：5 正交维度需 7 字段 context；多处差异化状态修改无法覆盖）。Phase 6.3 完成 ceiling 动态化（按方法分组描述，不再硬编码数值）。R9 合规修复已完成（`str(e)` → `DataSanitizer.sanitize_error(e)`）。 | 策略层重构或新增策略时统一走 `classify_error` + `classify_severity`。upgrade 触发条件：策略层重构时。评审决策：保持现状（NOTE(lazy) 标记已记录 upgrade 条件），强行接入 classify_error 会改变 system severity → raise 行为，违反 §1.4「不做无益重构」。 |
| **P3** | **utils/ 层 except Exception 已标记 NOTE(lazy)，待评估统一走 classify_error** | R3 场景遗漏检视发现：utils/ 层 except Exception（分布在 config_handler / exception_hooks / logger / singleton_registry / diagnostics 等）。已统一标记 `# NOTE(lazy):`（三要素齐全：简化内容 + ceiling + upgrade），`time_utils.py` 中一处候选标记已剔除（无业务消费方语义，调查时判定不适合标记）。0 处适合走 `classify_error`（均无业务消费方，属基础设施兜底或合理降级）。 | 后续 utils 层异常处理统一改造时重新评估是否引入 `classify_error`。upgrade 触发条件：utils 层异常处理统一改造时。 |
| **P3** | **红线自动化覆盖部分实现（R1/R4/R12/R13/R14/R15 已落地，R16 暂缓）** | CLAUDE.md §3.1 中 R1/R4/R12/R13/R14/R15/R16 标注「可自动化待实现」。R1 已由 import-linter 契约守护（4 条禁止导入契约）。R4/R12/R13/R14/R15 已由 `scripts/check_redlines.py` 实现，接入 `.pre-commit-config.yaml` `redline-check` hook（单元测试守护，数量见 CI 日志）。R16（UI 阻塞）暂缓：AST 扫描误报风险高，需更精确的事件处理器识别逻辑。 | R16 暂缓。upgrade 触发条件：R16 误报控制方案成熟或红线违规频发时重新评估。 |
| **P3** | **litellm 上游限制 `Requires-Python <3.14`** | litellm 1.83.8+（含项目锁定的 1.92.0）声明 `Requires-Python >=3.10,<3.14`。Python 3.14 下 `uv pip install -r requirements.txt` 因 litellm 无法安装而失败，导致 `ci-checks` 整个 job 全链路中断（依赖安装 → pre-commit → pip-audit → pyright → 测试 → 迁移 → 覆盖率均无法运行）。当前状态：`lint-fast` job matrix 仍含 `['3.13', '3.14']`，其中 `3.14` 标记 `experimental: true` 并 `continue-on-error`，仅跑 `ruff check` + `ruff format --check`，不安装项目依赖；完整测试矩阵（`ci-checks` 等）仅运行 Python `3.13`。降级到 1.83.7 不可接受（丢失 aiohttp CVE-2026-47265/CVE-2026-34993 修复）。2026-07-18 复核：litellm 最新版本 1.92.0（2026-07-12 PyPI 发布，元数据已核实）仍声明 `Requires-Python <3.14, >=3.10`，限制未解除。 | litellm 发布解除 `<3.14` 限制的版本时：① 升级 `pyproject.toml` 的 litellm 版本；② 重新 `uv pip compile` 生成 requirements*.txt；③ 将 `3.14` 从 `lint-fast` 的 experimental 矩阵项转为完整测试矩阵项。upgrade 触发条件：litellm 解除 `<3.14` 限制。 |
| **P3** | **AIStrategyMixin（2173 行）高度内聚，context builder 拆分推迟评估** | `strategies/ai_mixin.py` 的 `AIStrategyMixin` 当前 2173 行（2026-07-18 复核：旧记录 1890 行已过时），承载 AI 策略 context 构建与决策调用，方法间共享状态密集、内聚度高。Phase 评审判定当前规模未触发可维护性红线（无跨策略复制粘贴、无重复 context 拼接逻辑），强行拆分会引入跨模块状态传递抽象，违反 §1.3「拒绝过度抽象」。文件增长单独不触发 upgrade 条件。 | 未来 AI 策略重构或新增 AI 策略类型时再评估拆分 context builder。验收标准：① context builder 拆为独立模块/类（如 `AIContextBuilder`）；② 拆分后 AI 策略行为不变（现有 AI 策略单测 + 集成测试全过）；③ 新增 context builder 单测覆盖核心分支。upgrade 触发条件：AI 策略重构或新增 AI 策略类型时。 |
| **P3** | **TushareClient（2116 行）capability probe 部分约 400 行可独立拆分** | `data/external/tushare_client.py` 的 `TushareClient` 当前 2116 行（2026-07-18 复核：旧记录 1852 行已过时），其中 capability probe（能力探测）部分约 400 行与主客户端职责正交，可独立拆分。当前未拆分原因：probe 完成后需通知上层刷新配置/缓存，缺少回调通知机制；强行拆分会引入循环依赖或全局状态。Phase 评审判定为推迟优化（YAGNI：当前 probe 部分稳定，无频繁变更）。文件增长单独不触发 upgrade 条件。 | capability probe 频繁变更或新增 probe 维度时拆分。验收标准：① probe 部分拆为独立模块/类（如 `TushareCapabilityProbe`）；② 引入回调通知机制（probe 完成/失败通知 `TushareClient` 上层）；③ 拆分后 `TushareClient` 单测全过；④ 新增 probe 模块单测覆盖各 API 探测分支。upgrade 触发条件：capability probe 频繁变更或新增 probe 维度时。 |
| **P3** | **CacheManager（1321 行）作为薄包装 facade 不主动拆分** | `data/cache/cache_manager.py` 的 `CacheManager` 当前 1321 行（2026-07-18 复核：旧记录 1109 行已过时，文件增长到 1321 行，仍属薄包装 facade，无业务逻辑泄漏迹象），但属薄包装 facade：方法多为对 DAO 实例/引擎生命周期的转发，无独立业务逻辑。Phase 评审判定按 §1.3「拒绝过度抽象」与 §4.3「单例模式」不主动拆分——拆分薄包装 facade 会增加间接调用层而无实质价值。 | 不主动拆分。仅在 facade 表面出现责任漂移（非缓存职责混入）或新增多个非薄包装逻辑时重新评估。验收标准：① 评估时确认仍是薄包装 facade（无业务逻辑泄漏到 facade）；② 如确有拆分必要，按职责拆分并保留 facade 兼容性（不破坏调用方）；③ `CacheManager` 单测 + `_reset_singleton` 隔离测试全过。upgrade 触发条件：facade 出现责任漂移或新增非薄包装逻辑时。 |
| **P3** | **R13 自动化增强（_create_engine/close 维度检查）为可选优化** | `scripts/check_redlines.py` 当前 R13 守护已落地：检查新增 DAO 在 `CacheManager.__init__` 中实例化。但 R13 红线全条款还要求「在 `_create_engine` 中更新 `.engine` 引用」与 close 路径正确释放，这两维度未做静态检查。Phase 评审判定为可选优化：① `_create_engine` 与 close 维度检查需 AST 解析方法体（复杂度高于 `__init__` 实例化检查）；② R5「僵尸引擎操作」运行时已有人工评审兜底；③ 当前未发生该维度漏报。 | 可选优化，不主动实施。新增 DAO 未在 `_create_engine` 更新 `.engine` 引用或未在 close 中正确释放导致 R5 问题时实施。验收标准：① `check_redlines.py` 增加 `_create_engine` / close 维度的 AST 检查；② 新增对应单元测试（正例 + 反例）；③ CI `redline-check` hook 通过。upgrade 触发条件：R13 这两维度出现漏报或 R5 僵尸引擎问题频发时。 |
| **P3** | **R16 AST 守卫维持暂缓，依赖 @log_async_operation 运行时检测** | CLAUDE.md §3.1 R16「UI 阻塞主循环」标注「可自动化待实现（AST 检查，暂缓：误报风险高）」。Phase 评审决策维持暂缓：① AST 扫描难以精确识别 Flet 事件处理器边界（onClick/on_change 等回调 vs 普通方法），误报率不可控；② 项目已有 `@log_async_operation(threshold_ms=PerfThreshold.XXX)` / `@track_performance()` 运行时检测慢操作告警，R16 漏报会被运行时告警捕获。本条与上表「红线自动化覆盖部分实现」条目互补：前者讲整体进度，本条专门记录 R16 维持暂缓的决策依据。 | 维持暂缓。误报控制方案成熟或 R16 运行时检测漏报频发时重新评估。验收标准：① AST 守卫接入 pre-commit `redline-check` hook；② 误报率 < 5%（在现有事件处理器样本上验证）；③ 不漏报已知的同步阻塞事件处理器；④ 新增对应单元测试（正例 + 反例 + 边界）。upgrade 触发条件：R16 误报控制方案成熟或运行时检测漏报频发时。 |
| **P3-WinE2E-Skip** | **Windows E2E 关键路径 skip（onboarding DB 成功 / settings log level 切换）** | Windows 平台 Flet/Playwright CanvasKit textbox 渲染 + 向导状态隔离 + snackbar 时序 + select_dropdown 性能问题，导致 `test_wizard_db_validation_success` 和 `test_settings_log_level_switch` 两个 E2E 用例在 Windows 下不稳定（30+ 分钟耗时或超时）。当前用 `@pytest.mark.skipif(sys.platform == "win32", ...)` 单层装饰器跳过（已清理历史双重 skip），并在 `tests/integration/` 补集成测试覆盖 ViewModel/service 路径。2026-07-18 复核：Flet 已升级到 0.86.0（`pyproject.toml` 的 `[project.dependencies]` 锁定 `flet==0.86.0` / `flet-desktop==0.86.0` / `flet-charts==0.86.0`），但未在 Windows 环境重新验证这两个 skip 用例是否仍触发不稳定。下次 Windows E2E 维护时需重新验证并对应更新 reason 文本。 | ① Flet 升级到修复 CanvasKit textbox 渲染问题的版本；② 引入 headless 浏览器方案替代 CanvasKit；③ Playwright snackbar 时序问题在上游修复。upgrade 触发条件：Flet/Playwright 升级或 Windows E2E 稳定性方案落地时重新评估。 |
| **P3-E2E-Headless** | **E2E 测试 30 errors（Flet CanvasKit headless 限制 + DB 创建机制缺失 + asyncpg/ProactorEventLoop 兼容性）** | Phase 7.2 全量回归时发现 30 个 E2E 测试 ERROR：① 22 个 `NotImplementedError`（Flet CanvasKit 在 headless 环境下 `page.run_task` 等方法未实现，依赖 Flet 升级）；② 7 个 settings_tabs E2E `RuntimeError: E2E seed aborted: DB migration failed: connection was closed in the middle of operation`（错误消息文本与 asyncpg `ConnectionDoesNotExistError` 吻合，未直接捕获异常类）；③ 1 个与 R8 DeprecationWarning 相关（历史提交已修复）。**2026-07-19 根因研究（高置信度，基于代码现场分析）**：7 个 settings_tabs ERROR 双根因——根因 1（主因）：E2E 强制用 `ProactorEventLoop`（`tests/e2e/conftest.py` 的 `pytest_asyncio_loop_factories` hook，subprocess.Popen + Playwright 需要），但 asyncpg 的 socket I/O 与 Proactor 的 IOCP 模型不兼容，抛 `ConnectionDoesNotExistError`；根因 2（协同因）：`tests/integration/conftest.py` 的 DB 创建机制（`_ensure_test_db` + `db_schema_ready` autouse fixture）因 pytest conftest 作用域规则不对 `tests/e2e/` 生效，`_seed_e2e_data` 只调 `DatabaseMigrator.init_db`（建 schema）不创建 DB 本身，worker 若只跑 E2E，`test_astock_<worker>` DB 永远不会被创建。**已排除根因**：xdist 并行 worker 竞争（per-worker DB 隔离 `TEST_DB_NAME` 默认派生为 `test_astock_{_xdist_worker}` 已实现，且 7 个测试通过 `xdist_group("settings_e2e")` 同 worker 串行）；`_alembic_lock` 跨 worker 无效（同 worker 内 session-scope seed 与 function-scope 测试不并发）；Flet 子进程与 seed 并发（`flet_app` 依赖 `seed_e2e_data`，子进程在 seed 后启动）。**修复方案（已实施，混合方案）**：采用 spec 方案 2A 的 autouse fixture 机制 + 方案 1A 的线程隔离原则（跳过 spec 的「先试 2A」步骤，因根因 1 确认为主因，直接调用 `_ensure_test_db()` 必然失败）。具体实施：① 在 `tests/e2e/conftest.py` 添加 `_ensure_e2e_db` session-scope autouse fixture，通过 `asyncio.to_thread` + 独立 `SelectorEventLoop` 在新线程中跑 `_ensure_test_db`；② 修改 `seed_e2e_data` fixture 显式依赖 `_ensure_e2e_db`，并将 `_seed_e2e_data()` 调用同样包装在 `asyncio.to_thread` + `SelectorEventLoop` 中（因 `_seed_e2e_data` 内部 `DatabaseMigrator.init_db` 与 `asyncpg.connect` 均受根因 1 影响）；不修改全局 `event_loop_policy`，避免污染主线程。设计 spec（未纳入版本控制）阶段 1。**验证状态**：本地 Windows 验证因 PostgreSQL 18 `postgres` 用户密码与 conftest.py 派生密码（`hashlib.sha256(f"astock_local_{_local_user}".encode()).hexdigest()[:24]`）不匹配阻塞（环境配置问题，非代码缺陷），跳过本地验证信任 CI 验证。 | ① ~~7 个 settings_tabs E2E ERROR 根因研究~~（**已完成**）+ ~~修复方案实施~~（**已完成，待 CI 验证通过后关闭**）；② 22 个 CanvasKit headless `NotImplementedError` 仍依赖 Flet 升级；③ 或引入 Playwright headless 浏览器方案替代 Flet 自带的 CanvasKit。upgrade 触发条件：① Flet 升级到修复 CanvasKit headless `NotImplementedError` 的版本；② 或引入 Playwright headless 浏览器方案替代 Flet 自带的 CanvasKit。 |
| **P3-UI-Source-Bugs** | **Phase 6.5 E2E 测试发现 6 个 UI 源码 bug（②⑥ 待 Flet 升级，①③④⑤ 已解决）** | Phase 6.5 设置面板 E2E 测试编写过程中发现 6 个源码 bug，按"补测任务不修复源码"原则未在 Phase 6 修复。技术债清理 Plan 已解决 ①③④⑤：① `ToastManagerView` 挂载（Task 2.1, commit 35d07b00）；③ `AIService.test_connection` 返回 i18n key + VM 用 `Message(key)` 消费（Task 3.1-3.3, commits 075858d2/df76f3c3/85241edf）；④ Excel 导出实现（Task 4.1-4.4, commits e8682f0c/ebca6717/63358adf/4aff5580，openpyxl + `write_excel` + 双格式导出按钮）；⑤ `AlertDialog modal=False` + `on_dismiss` 回调（Task 5.1, commit 91f9006c）。剩余 ②⑥ 待 Flet 升级：② `fill_textbox` 在 CanvasKit 模式下不生效（keyboard.type 不触发 Flutter TextEditingController 更新）；⑥ `Flet FilePicker.save_file` 无法被 Playwright 捕获 download 事件。2026-07-18 复核：Flet 已升级到 0.86.0（`pyproject.toml` 的 `[project.dependencies]`），但 bug ②⑥ 未在 0.86.0 下重新验证是否仍存在。下次 E2E 维护时需重新验证并对应更新本条目状态。 | ② 等待 Flet 修复 CanvasKit fill_textbox 或改用其他输入方法；⑥ 等待 Flet FilePicker.save_file Playwright 支持。upgrade 触发条件：Flet 升级时。 |
| **P3-Duplicate-Batch-Get** | **DAO batch get 同构模板跨多文件重复（2026-07-19 复核：13 方法/7 文件）** | `data/persistence/daos/` 下 7 个 DAO 文件含 13 个 `get_*_batch(ts_codes, as_of_date=None)` 同构方法：`financial_dao.py`（get_financial_reports_history_batch/get_fina_audit_batch/get_dividend_batch/get_pledge_stat_batch/get_fina_forecast_batch/get_fina_mainbz_batch）、`holder_dao.py`（get_top10_holders_batch/get_stk_holdernumber_batch）、`express_dao.py`（get_express_batch）、`pledge_detail_dao.py`（get_pledge_detail_batch）、`share_float_dao.py`（get_share_float_upcoming_batch）、`stk_holdertrade_dao.py`（get_stk_holdertrade_batch）、`top_inst_dao.py`（get_top_inst_batch）。模板结构高度同构：① 入参签名固定 `ts_codes: list[str], as_of_date=None`；② 早空返回 `pd.DataFrame()`；③ `try: chunked_in_query(self._read_db, sql_fn_or_str, ts_codes, params_fn=...)` 双分支（as_of_date 是否为 None）；④ `except asyncio.CancelledError: raise` + `except EngineDisposedError: raise` + `except Exception: logger.warning + return pd.DataFrame()` 兜底。强行抽象会引入 `BatchGetTemplate` 通用基类 + SQL 模板 DSL，违反 §1.3「拒绝过度抽象」（单实现的接口/永不变化的配置），故推迟。 | DAO 同构查询参数化提取为公共模板（如 `BaseDao._batch_get_with_as_of_date(sql_fn, ts_codes, as_of_date, log_prefix)`），消除双分支重复。验收标准：① 抽取后 13 方法行为不变（现有 DAO 单测全过）；② 新增模板单测覆盖 as_of_date 双分支 + 空入参 + 异常兜底。upgrade 触发条件：新增第 14 个同构 batch get 方法时提取公共模板。 |
| **P3-Duplicate-VM-Helpers** | **4 config panel VM 助手 `_show_error`/`_show_warning`/`_raw_message` 逐字节相同** | `ui/viewmodels/` 下 4 个 config panel ViewModel（`LocalModelConfigPanelViewModel`、`TushareConfigPanelViewModel`、`LLMConfigPanelViewModel`、`DatabaseConfigPanelViewModel`）各含 3 个同构助手方法：`_show_error(message)` = `_set_state(status_message=message, status_type="error")`；`_show_warning(message)` = `_set_state(status_message=message, status_type="warning")`；`_raw_message(text)` = `Message(_RAW_MSG_KEY, {"default": text})` 包装动态字符串。12 个方法实现逐字节相同（仅 llm_config 多一个 `_show_info`）。当前不抽象因 §1.3「拒绝过度抽象」（单次使用的辅助函数独立模块）；4 文件 ×3 方法尚未触发提取阈值。 | 提取公共混入（如 `ConfigPanelStatusMixin` 含 `_show_error`/`_show_warning`/`_raw_message`），4 VM 继承混入消除重复。验收标准：① 4 VM 行为不变（现有 VM 单测全过）；② 混入单测覆盖 3 助手。upgrade 触发条件：新增第 5 个含同构 `_show_error`/`_show_warning`/`_raw_message` 助手的 VM 时提取公共混入。 |
| **P3-Tushare-Token-Invalid-Race** | **`tushare_client._token_invalid` 跨同步/异步路径无同步读写（NOTE(lazy) 三要素齐全但未收录债表）** | `data/external/tushare_client.py` 的 `TushareClient._token_invalid: bool` 是全局 token 熔断标志，跨同步路径（`set_token()` 重置为 False）与异步路径（`_query_with_retry` 内 token 认证失败时置 True）读写，未持锁（async 路径不能持 `threading.Lock`，会阻塞事件循环）。代码现场已标 `# NOTE(lazy): _token_invalid 读写未持锁（async 路径不能持 threading.Lock）. ceiling: set_token 后被旧协程覆盖为 True 导致持续误熔断（极窄竞态窗口，需用户再次 set_token 自愈）. upgrade: 改用 asyncio.Lock 或 atomic 标志位（如 asyncio.Event）替换 bool 字段.`（三要素齐全：简化内容 + ceiling + upgrade），但未收录本债表，导致跨检视周期不可追溯。本次仅补登记，不改代码。 | 改用 `asyncio.Lock` 保护 `_token_invalid` 读写，或改用 `asyncio.Event` 替换 bool 字段（语义：set=熔断 / clear=恢复 / wait=阻塞直到恢复）。验收标准：① `_token_invalid` 读写均经锁/Event 保护；② 现有 TushareClient 单测全过；③ 新增并发竞态单测（旧协程在 set_token 后将 _token_invalid 覆盖为 True 的场景）。upgrade 触发条件：触发误熔断竞态（用户报告 token 已更新但仍 fast-fail）或升级 asyncio.Lock 时。 |
| **P3-ScreenerVM-I18n-Get-Residual** | **`screener_view_model.py` 3 处 I18n.get 残留（VM 感知 locale 偏离，NOTE(lazy) 同主题已达 upgrade 阈值）** | `ui/viewmodels/screener_view_model.py` 含 3 处 I18n.get 残留（违反 CLAUDE.md §3.2「VM 不感知 locale；VM 只产出 i18n key 与 params，View 按当前 locale 渲染」）：① `ScreenerViewModel.get_strategy_desc` 调 `I18n.get(st.desc_key)` 返回翻译字符串供 `update_strategy_desc` 拼接 `state.strategy_desc`（str 字段）；② `ScreenerViewModel.update_strategy_desc` 调 `I18n.get('strategy_missing_apis')` 拼入 `warning_suffix` 形成翻译字符串 desc；③ `ScreenerViewModel.set_history_viewing_status` 的 `label` 入参为 View 层 `translate_strategy_name` 已翻译字符串（VM 不应接收已翻译值），存入 `state.status_message.params["label"]`。3 处均带 `# NOTE(lazy)` 或 `# I18N_GET_ALLOWED` 标注，ceiling 均为「VM state 非 Message 结构 / 未引入 strategy_name_key 机制」，upgrade 均为「R.3 strategy_name 标准化启动时统一改造」。同主题 NOTE(lazy) 已达 3 处（≥3 触发升级为债表条目，按 CLAUDE.md §3.3 规则），本次补登记。 | R.3 strategy_name 标准化启动时统一改造：① `state.strategy_desc` 改为 `Message` 结构（desc_key + params），View 渲染时翻译；② `strategy_obj.get_dynamic_description(params)` 返回值改为 i18n key（而非翻译字符串）；③ `translate_strategy_name` 迁入 VM 或引入 `strategy_name_key` 机制，`label` 改为传 raw strategy_name + View 渲染时翻译。验收标准：① 3 处 I18n.get 残留消除（VM 内无 I18n.get 调用）；② 现有 screener_view_model 单测全过；③ 新增 locale 切换自动刷新单测。upgrade 触发条件：R.3 strategy_name 标准化启动时统一改造。 |
| **P3-doc-E3-Term-Undefined** | **known-technical-debt.md 条目1 引用未定义术语 "E-3 决策"（已解决）** | known-technical-debt.md 条目1「期望的最终解法」列原引用"E-3 决策：保持现状..."，但"E-3"未在本文档或其他 ADR 文档中定义，全仓 grep 无匹配。可能是内部评审会议编号但未文档化，导致决策追溯链断裂。本次检视 P3 文档死链组发现，条目1 决策摘要补全时引入了该术语。相关文件：docs/debt/known-technical-debt.md 条目1。 | **已解决（2026-07-22）**：将"E-3 决策"改为"评审决策"，消除未定义术语，保留决策内容。 |
| **P3-doc-Hardcoded-Line-Numbers** | **known-technical-debt.md 多处使用硬编码行号违反 CLAUDE.md §1（已解决）** | CLAUDE.md §1 第 15 行明确规定"长期文档引用用符号锚点（函数/类/常量名 + 相对描述），不用硬编码行号"。known-technical-debt.md 大量使用 `file.py:NNN` 形式的硬编码行号引用（如 `time_utils.py:80`、`pyproject.toml:25-27`、`tests/e2e/conftest.py:217-226` 等），随代码演进易漂移。本次检视 P3 文档死链组发现的独立存量问题，非本次修复引入。相关文件：docs/debt/known-technical-debt.md 全文。 | **已解决（2026-07-22）**：所有硬编码行号已改为符号锚点（类名/方法名/字段名 + 相对描述），符合 CLAUDE.md §1 原则。 |
| **P3-ci-Network-Marker-Unused** | **network marker 在 CI 中未被实际使用** | tests/e2e/test_settings_tabs.py 两测试补 `@pytest.mark.network` 后，CI 全部 4 处 pytest 调用（`.github/workflows/ci_cd.yml` 中 4 处 pytest 调用步骤）均无 `-m "not network"` 或 `--without-network` 选项，network marker 测试在 CI 中始终被执行。marker 仅作标记，未实际驱动 CI 跳过。本次检视 P3 测试基建组发现。设计意图可能是预留 marker 用于"未来 CI 离线模式扩展"，但当前无行为影响。相关文件：.github/workflows/ci_cd.yml、pyproject.toml、tests/e2e/test_settings_tabs.py。 | 二选一：(1) 在 marker 描述中注明"预留 marker，CI 当前始终执行"；(2) 在 ci_cd.yml 添加 `-m "not network"` 选项逻辑，配合双路径容错设计在离线模式跳过。upgrade 触发条件：CI 离线模式扩展需求出现时。 |
| **P3-security-URL-Credential-No-Username** | **`_PATTERN_URL_CREDENTIALS` 不匹配无用户名 URL 凭证** | utils/sanitizers.py 的 `_PATTERN_URL_CREDENTIALS` 正则用户名捕获组 `([^:@\s]+)` 要求至少 1 字符，对 `redis://:password@host:6379` 这类无用户名的合法 URL 凭证不匹配，密码会原样保留在 `sanitize_error` 输出中，存在 R9 敏感信息泄露通道。本次检视 P3 安全卫生组发现。当前项目无 redis/mysql/mssql 等使用无用户名 URL 凭证的连接，故为潜在风险而非实际 bug。该 gap 在 P3-3 scheme 通配化改动之前就存在，非本次改动回归。相关文件：utils/sanitizers.py、tests/unit/utils/test_sanitizers.py。 | 将 `([^:@\s]+)` 改为 `([^:@\s]*)`（允许空用户名）。注意 re.sub 替换串 `\1://\2:***@` 对空用户名也成立（输出 `redis://:***@host`）。配套补负向测试用例固化行为。upgrade 触发条件：引入无用户名 URL 凭证连接（如 redis/mysql）时或安全审计时。 |
| **P3-security-Sanitize-Args-Non-String-Repr** | **`sanitize_args` 对非字符串参数 repr 未做 secret 替换** | utils/sanitizers.py 的 `sanitize_args` 函数 secret 替换仅对 `isinstance(arg, str)` 触发。若被装饰函数接收含 secret 字段的 dict/list 位置参数，会走 `repr(arg)[:100]` 分支，已注册 secret 可能落入前 100 字符未被脱敏。例如 `arg = {"token": "sk-xxxxxxxxxxxxxx"}` 且 secret 已注册，`repr(arg)[:100]` 会包含明文 token。本次检视 P3 安全卫生组发现。生产代码（ai_service.py、bootstrap.py 等）所有 `@log_async_operation` 均使用默认 `log_args=False`，`sanitize_args` 实际不被生产调用，故为潜在风险而非实际 bug。相关文件：utils/sanitizers.py、tests/unit/utils/test_sanitizers.py。 | 在 else 分支替换前对 `repr(arg)` 也跑一遍 `_known_secrets` 精确替换，参考实现（用 `<br>` 分隔的伪代码）：<br>`else:`<br>`    safe_repr = repr(arg)[:100]`<br>`    for _secret in list(DataSanitizer._known_secrets):`<br>`        if _secret in safe_repr:`<br>`            safe_repr = safe_repr.replace(_secret, "***")`<br>`    clean_args.append(safe_repr)`。upgrade 触发条件：`log_args=True` 在生产代码中启用时或安全审计时。 |
| **P3-test-MergedCov-Isolation** | **合并覆盖率运行时 12 个测试间歇性失败（test isolation/order-dependent）** | superpowers 元检视阶段发现：合并跑 `--cov` + xdist 时，12 个测试间歇性失败（`tests/integration/test_data_trade_calendar.py` 9 个 offline fallback、`tests/integration/test_data_processor.py::TestDataProcessor::test_get_trade_dates_fallback`、`tests/integration/test_scheduler_service.py` 2 个 nightly prediction），错误签名两类：① offline calendar 返回空结果（疑似 `OfflineCalendar` 单例/缓存被前序测试污染）；② `AttributeError: 'module' object at data.persistence.review_manager has no attribute 'review_manager'`（疑似 `monkeypatch.setattr("data.persistence.review_manager.ReviewManager", ...)` 在某测试执行后未还原，或 `sys.modules` 污染）。**已排除回归**：13 个失败测试单独运行 13/13 passed；单元测试单独运行 9877 passed；集成测试不需 DB 部分 128 passed；subagent 复现合并模式 + DB 不可用时，唯一不需 DB 的 `test_get_trade_dates_fallback` PASSED（直接证伪代码回归）。`pytest-randomly` 已安装导致每次运行顺序不同，间歇性失败特征吻合。本次修复（异常处理 + 日志脱敏）不涉及测试基建/`OfflineCalendar`/`review_manager` 模块结构，与该问题无因果链。相关文件：tests/integration/test_data_trade_calendar.py、tests/integration/test_data_processor.py、tests/integration/test_scheduler_service.py、data/domain_services/offline_calendar.py、data/persistence/review_manager.py。 | 排查两类泄漏源：① `OfflineCalendar` 是否有模块级缓存被前序测试污染（如 fixture 清理不彻底）；② `data.persistence.review_manager` 模块属性在某测试 `monkeypatch.setattr` 后未还原（注意 `sys.modules` 污染路径）。验收标准：① 用 `--randomly-seed=<失败种子>` 复现后修复，确保连续 10 次随机种子下全部通过；② 或加 `-p no:randomly` 确定性顺序跑也通过；③ 失败测试对应的 fixture/monkeypatch 复原点加固。upgrade 触发条件：合并覆盖率运行出现 2 次以上同种子复现失败，或 CI 出现间歇性失败影响 merge。 |
| **P3-WeakIsNotNone-Cleanup** | **123 处 `assert X is not None` 真弱断言（跨 40+ 文件，扫描器 BUG 修复后浮出）** | `scripts/scan_weak_assertions.py` 的 `_is_is_not_none_assert` 函数实现与意图相反（第 274 行误用 `ast.Is` 应为 `ast.IsNot`），导致 `weak_is_not_none` 规则从加入起从未生效，123 处真违规一直漏报。BUG 修复后扫描结果：baseline 从 602 → 741（+139），其中 +123 为 `weak_is_not_none`（漏报浮出），+16 为其他类型小幅增长（`weak_mock` +5 / `weak_raises_only` +10 / `weak_called_flag` +1，源于扫描器其他逻辑微调）。本次修复仅修扫描器 BUG + 重新生成 baseline（含 `growth_justification` 字段声明合理增长），123 处真弱断言按 CLAUDE.md §1.7「举一反三」规则（>3 文件跨多层须记为独立重构任务延后处理）记为独立任务。违规模式：`test_` 函数内 `assert X is not None` 作为唯一终态断言，仅验证存在性不验证行为/参数。相关文件：tests/unit/test_base_dao.py 等高发违规文件（40+ 文件，详见 `tests/weak_assertion_baseline.json` 的 `weak_is_not_none` 类型条目）。 | **已完成（2026-07-20）**：123 处 `weak_is_not_none` 弱断言全量治理完成。替换强断言 116 处（A1 isinstance 类型断言 70+ 处 / A2 值断言 20+ 处 / A4 容器断言 1 处 / A5 callable 断言 10+ 处），保留白名单 7 处（B 模式：tzinfo 时区感知契约 3 处 / bgcolor with_opacity 返回值类型不固定 1 处 / UI 契约测试多 key/label 循环验证 text_field 存在性 3 处）。治理跨 33 个测试文件（Batch 1: 10 文件 61 处 + Batch 2: 17 文件 39 处 + Batch 3: 16 文件 16 处 + 跨批次补漏 7 处），全部通过 pre-commit hook（ruff check / ruff format / type:ignore reason / check_redlines）+ 增量门禁（baseline 741→618，下降 123 条，weak_is_not_none 类型条目清零）。验收标准全部满足：① 每处替换为强断言或加白名单；② baseline `weak_is_not_none` 条目数下降至 0；③ 治理涉及的 33 文件相关单测全过；④ `--base` 增量门禁通过。 |
| **P3-Tushare-Client-Lazy-Markers** | **`tushare_client.py` 2 处 NOTE(lazy) 未收录债表（pro 类型注解 + points_15000 API 集）** | `data/external/tushare_client.py` 含 2 处 `# NOTE(lazy):` 标记（三要素齐全）但未收录本债表，导致跨检视周期不可追溯：① `TushareClient.pro` 字段声明为非 Optional 的 `TushareProApi` 以避免 40+ 处 `reportOptionalMemberAccess` warning，但运行时可能为 None（token 未设置时），调用方在 `_handle_api_call` 内部已有 `if not self.pro: raise` 检查，仅当调用方直接访问 `self.pro.xxx` 时会抛 AttributeError 而非友好错误；② `_TIER_API_COVERAGE["points_15000"]` 当前与 `points_10000` API 集相同（Tushare 暂无 15000 积分专属 API，特色数据需独立购买）。第三处标记 `_token_invalid` 读写未持锁已收录为 P3-Tushare-Token-Invalid-Race。本次仅补登记，不改代码。 | ① `pro` 字段改用 `_get_pro() helper` 方法返回 `TushareProApi`（内部 raise 友好错误），调用方统一改用 helper；② `points_15000` 在 Tushare 新增 15000 积分专属 API 时追加 API 集合。验收标准：① 现有 TushareClient 单测全过；② `pro` 字段改造后调用方无新增 `# type: ignore`；③ `points_15000` 改造需配套单测覆盖新 API 探测分支。upgrade 触发条件：① `pro` 字段访问引发 AttributeError 排查需求时；② Tushare 新增 15000 积分专属 API 时。 |

## 引用关系

- [CLAUDE.md §3.3](../../CLAUDE.md#33--已知技术债与架构限制-known-limitations): 已知技术债与架构限制索引（CLAUDE.md 仅保留摘要 + 指向本文的链接）
- [CONTRIBUTING.md §排查典型问题](../../CONTRIBUTING.md#排查典型问题): 排查典型问题（与技术债互补）
- [docs/guides/ci-cd.md](../guides/ci-cd.md): CI/CD 流水线与门禁（litellm Python 3.14 限制相关 CI 矩阵配置）
