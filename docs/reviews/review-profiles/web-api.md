# Web/API 专项 Profile

> 加载方式：由 [ai-review.md §6](../ai-review.md#6-风险信号--专项-profile-触发表) 触发。只补充通用维度的增量风险。

## 检视要点

- 路由、参数绑定、内容类型和请求大小是否正确处理；
- 对象级授权（不仅认证）、CORS、CSRF 和限流是否到位；
- 状态码、错误体、分页、排序和缓存头是否语义正确；
- 上传下载、流式响应、慢客户端和断连是否安全处理；
- 反向代理头的信任边界是否正确（X-Forwarded-For 等）。
