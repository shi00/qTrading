## 🎯 PR 概述
落实 FR-UX-005（Task 4.3）：将选股历史表的复盘数据（prediction_result / t1_pct / t5_pct / alpha / params_snapshot）从隐藏列转为可见，并在股票详情对话框新增"复盘"区，包含 WIN/LOSS 徽章、T+1/T+5/Alpha 带符号百分比、参数快照结构化展示。让用户能感知每次选股的预测结果与实际表现，闭合"预测→复盘"环路。

## 🔗 关联工单/Issue
Relates to #340 (URD v2.6 FR-UX-001~006 落档)
Relates to Plans.md Phase 4 Task 4.3

## 📌 修改类型
- [x] feat：新增业务功能

## 📝 详细变更清单
- ui/views/screener_view.py
  - `_HIDDEN_COLS` 移出 `prediction_result` / `t1_pct` / `t5_pct`（`alpha` 原本就可见），新增 `params_snapshot` 到隐藏列（详情对话框展示）
  - 新增 `_PCT_COLS = {"t1_pct", "t5_pct", "alpha"}` 常量
  - `_COLUMN_WIDTHS` 补 `prediction_result:80` / `t1_pct:80` / `t5_pct:80` / `alpha:80`
  - `_format_cell_value` 新增 `prediction_result` 分支（WIN→"胜" / LOSS→"负" / 其他→"-"），`_PCT_COLS` 走带符号格式化（+1.23 / -1.23 / 0.00）
- ui/components/stock_detail_dialog.py
  - 新增模块级纯函数：`_build_prediction_badge` / `_format_pct_with_sign` / `_has_review_value` / `_build_params_snapshot_section` / `_build_review_section`
  - `_build_content` 末尾追加 `_build_review_section(stock_data)`，渲染徽章 + T+1/T+5/Alpha chips + 参数快照（dict/JSON 字符串/None 三态）
- locales/zh_CN/strings.json + locales/en_US/strings.json
  - 新增 i18n key：`prediction_win`（胜/Win）、`prediction_loss`（负/Loss）、`review_section_title`（复盘/Review）
- tests/unit/ui/test_screener_view.py
  - `TestFormatCellValue` 新增 `prediction_result` WIN/LOSS/None/UNKNOWN 四态 + `t1_pct`/`t5_pct`/`alpha` 正/负/零带符号格式化测试
  - 新增 `TestHiddenColsContract` 守护 `_HIDDEN_COLS` 契约（复盘列可见、`params_snapshot`/`t1_price`/`t5_price` 隐藏）
- tests/unit/ui/test_stock_detail_dialog.py
  - 新增 `TestBuildPredictionBadge` / `TestFormatPctWithSign` / `TestHasReviewValue` / `TestBuildParamsSnapshotSection` / `TestBuildReviewSection` 测试类，覆盖三态分支与边界（None/NaN/空字符串/非 JSON 字符串/空 dict）

## 🧪 测试覆盖
### 自动化测试
- [x] 单元测试：新增 30+ 用例覆盖纯函数三态分支与契约守护，本地 `pytest tests/unit/ui/test_screener_view.py tests/unit/ui/test_stock_detail_dialog.py` 186 passed
- [x] 静态检查：ruff check + ruff format + pyright 全过
- [ ] CI流水线全部通过（lint / test / build / windows e2e）— 待 CI 跑完

### 手动验证
- [x] 数据库迁移回滚验证 — 本 PR 无 DDL 变更
- [x] UI 变更附前后对比说明 — 见下方"评审重点"

## ⚠️ 风险与兼容说明
1. **破坏性变更**：无。仅 UI 渲染层修改，未改 DB schema / 接口契约。
2. **数据库迁移**：不需要。
3. **定时调度/后台任务**：不影响。
4. **安全相关**：无变更。

## 📖 文档同步校验
- [x] CLAUDE.md — 无需更新（未改架构/单例/策略规范）
- [x] CONTRIBUTING.md — 无需更新（未改流程/安装/测试步骤）
- [x] CHANGELOG.md — 由 release-please 自动生成

## ✅ 提交前自检清单
- [x] 代码符合 CLAUDE.md 架构红线 R1-R18（Import Linter / Redline check 全过）
- [x] 无硬编码密钥、数据库密码、Token、隐私信息
- [x] 无废弃死代码、注释掉的临时调试代码
- [x] 所有新增分支、边界逻辑配套对应测试用例（30+ 新增测试覆盖三态）
- [x] 国际化文案统一使用 I18n，无中文硬编码（`prediction_win` / `prediction_loss` / `review_section_title` 双语齐全）
- [x] 并发逻辑无 pd.options 全局竞态污染
- [x] 数据库新增字段补充索引、非空、默认约束 — N/A（无 DDL）
- [x] 已确认避免重复造轮子（`_info_chip` 复用已有 chip 组件）
- [x] 本次修改引入的新问题已全部自查修复
- [x] Ruff lint + format 已通过
- [x] Pyright 类型检查已通过
- [x] 测试断言避免弱断言（用 `assert x == y` / `isinstance` 精确断言）
- [x] 所有 `# type: ignore` 均带 `[reason]` 注释（本 PR 无新增）
- [x] 所有 `# NOTE(lazy):` 标记含三要素（本 PR 无新增）

## 👀 评审重点提示
1. **`_format_cell_value` 的 `prediction_result` 分支位置**：放在 `pd.isna(val)` 之后、`_DATE_COLS` 之前，确保 None/NaN 先被拦截（测试 `test_prediction_result_none` 验证）。
2. **`_build_review_section` 空态降级**：所有复盘字段都为空时返回 `ft.Container()`（空容器），避免渲染空 section（测试 `test_no_review_data_returns_empty_container` / `test_all_none_returns_empty_container` / `test_all_nan_returns_empty_container` 验证）。
3. **`_build_params_snapshot_section` 三态**：dict 直接渲染键值对、JSON 字符串先 `json.loads` 解析（失败回退原始字符串）、None/空字符串返回空容器。
4. **`_HIDDEN_COLS` 契约**：`params_snapshot` 在表格中隐藏（详情对话框展示），`t1_price`/`t5_price` 保持隐藏（绝对价格无业务价值，仅 t1_pct/t5_pct 有意义）。
5. **WIN/LOSS 徽章着色**：WIN 用 `AppColors.SUCCESS` + CHECK_CIRCLE 图标，LOSS 用 `AppColors.ERROR` + CANCEL 图标，UNKNOWN/空用 `AppColors.TEXT_HINT` + REMOVE 图标。
